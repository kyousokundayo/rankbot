# 変更履歴

プレイヤーに見える変更をバージョンごとにまとめる。そのまま Discord へ貼れる
粒度で書く (内部の改善は「内部」節へ短くまとめ、告知では省いてよい)。

## v0.34

### 🎧 通話の並び順が処刑・襲撃で動かなくなりました

死亡した人の表示名を **`01.名前(処刑)`** の形に変えました。これまでは
`(処刑)01.名前` だったため、死んだ人だけが番号順から外れて通話画面の上や下へ
飛んでいました。番号が先頭に残るので、**最初から最後まで並びが変わりません。**

### 🔢 すべて番号順に並ぶようになりました

- 投票・決戦投票のボタン
- 占い・護衛・襲撃の選択
- 人狼DMの「他の人狼」
- 投票結果と投票内訳
- GMの参加者除外

これまでは参加順（＝番号がバラバラ）でした。`01` から順に並びます。

あわせて、投票内訳が**投票した順**に表示されていたのを番号順に直しました。
誰が先に投票したかが分からなくなります。

### 🌅 「朝を迎える」がDMから `#昼` に移りました

毎晩DMに届いていたパネルをやめ、**`#昼` に1枚だけ**出るようになりました。
夜の相談DMに埋もれて見失うことがなくなります。押し方はこれまでと同じで、
生存者全員が押すと朝になります。

**宣言した人数は夜のあいだ誰にも表示されません。** 押した人にも出ません。
夜に行動があるのは人狼・占い師・狩人だけなので、人数が見えると「まだ押して
いない役職者が何人いるか」から生存役職を推測できてしまうためです。**夜の
目安時間が切れた時点で、全員に同時に**表示されます。

### 🗳️ 投票したことが分かりやすくなりました

投票を確定すると、確認メッセージが **「✅ 05.名前 に投票しました。」** に
変わってその場に残ります。これまでは「投票しました」と一瞬出るだけでした。

### 📋 終了後の表示順を変えました

**進行ログ → 勝利陣営・全役職公開 → レート変動** の順になりました。結果と
レート変動が下にまとまるので、長い進行ログに押し流されません。

### 🆔 試合番号が 1 から連番になりました

統計の「最近の試合」「自分の履歴」で `3, 69, 70, 71` のように飛んでいた番号を
**`1, 2, 3, 4`** に振り直しました。欠番は開発中の動作検証で消費されたもので、
実際の試合ではありません。

### 内部

- 全員が同時に押すボタン（朝を迎える／役職を確認した／投票の確定）の所要時間を
  段階ごとに計測し、2秒を超えたらログへ残す。「押したのに反応しない」の原因を
  切り分けるため
- DBバックアップが `-wal` / `-shm` を残さないよう、作成時にチェックポイントし、
  世代を捨てるときは付随ファイルもまとめて削除する
- 再起動をまたいだ古い「朝を迎える」パネルを、次の掲示前に削除する
