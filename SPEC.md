# Discord 人狼Bot 仕様書 v3

策定日: 2026年6月 / 本書は**現行実装を正**として記述する。
旧仕様v2からの主な変更点は[末尾](#12-v2からの主な変更点)参照。

## 1. 概要

| 項目 | 内容 |
|---|---|
| プレイヤー人数 | 13人固定 |
| 操作方式 | ボタンUI主体 (スラッシュコマンドは管理用の3つのみ) |
| 卓 (同時開催) | 標準4卓 (初心者/中級者/上級者/総合) + 任意のローカル固定卓 + 村長専用村 |
| レーティング | 固定プール制レート + 相対ランク9段階 + シーズン制 |
| ゲーム用チャンネル | `#昼` をゲームごとに自動生成、結果発表300秒後に自動削除 |
| 統計・報告 | 既存の `#総合` 直下にある `#統計` (永続・ボタンUI) |
| 事前募集 | `#募集` で7日先まで予約し、開催時に既存の `#参加受付` へ移行 |
| 役職通知 | DM送信 (DM受信不可のユーザーは参加不可) |
| 障害復旧 | ゲーム状態をSQLiteへ常時保存し、Bot再起動後に途中再開可能 |
| 言語 | 日本語 |

## 2. 役職と勝利条件

役職配分 (13人): **人狼3 / 狂人1 / 占い師1 / 霊媒師1 / 狩人1 / 村人6**。
役職・番号の割り当ては `secrets.SystemRandom` (暗号学的乱数) を使用。

| 役職 | 陣営 | 能力 |
|---|---|---|
| 人狼 ×3 | 狼 | 夜にDMで相談 (自動中継) し、襲撃対象をUIで決定。役職DMで相方の名前を通知 |
| 狂人 ×1 | 狼 | 能力なし。狼が誰かは知らない。判定上は「村人」扱い |
| 占い師 ×1 | 村 | 夜に1人を占い、結果「**人狼 / 村人**」の2値をDMで受信。開始直後に初日ランダム白 (人狼と占い師自身を除く参加者から選出) を受信 |
| 霊媒師 ×1 | 村 | 処刑直後、処刑者が「**人狼 / 村人**」だったかをDMで受信 (役職そのものは通知しない) |
| 狩人 ×1 | 村 | 夜に1人を護衛。自分護衛不可・連続同一護衛不可 (選択肢から除外+エラー表示) |
| 村人 ×6 | 村 | 能力なし |

**勝利条件** (昼処刑直後・夜襲撃直後・プレイヤー除外直後に判定):
- 村陣営: 全人狼の死亡
- 狼陣営: 生存人狼数 ≧ 生存非人狼数 (狂人は非人狼として数える)

## 3. チャンネル・カテゴリ構成

```
村長ロール説明/           ← 当面はAdministrator権限保持者のみ表示
  #専用村作成            (専用村の作成/村名変更/削除ボタン)
#総合                    (既存の総合案内チャンネル)
#統計                    (#総合の直下。永続・書き込みはBotのみ)
#募集                    (永続。作成・参加・補欠・GM登録・卓への移行)
<既存カテゴリ>/             (Bot管理外。配置や名前を変更しない)
開発/
  #運営                  (Administratorのみ。拒否集計・GM解除・募集強制削除)
<卓名>/                  ← 卓ごとに1カテゴリ (表示権限は卓の参加条件に連動)
  #参加受付              (永続。参加/GM/開始/ヘルプボタン)
  🔊人狼ゲーム           (永続VC。ゲーム中の音声会話)
  #昼                    (ゲーム中のみ存在。議論・投票・結果発表)
  #霊界                  (ゲーム中のみ存在。死亡者+観戦者の雑談。生存者には見えない)
```

- 死亡者: `#昼` が読み取り専用になり、`#霊界` で観戦者と会話できる (死亡時にメンバー個別の閲覧ブロックが解除される)
- 観戦者 (未参加者): `#昼` は読み取りのみ、`#霊界` は書き込み可。ゲーム中はVCで発言不可 (§6)
- `#霊界` の実装: 部屋の表示権限をベースに、**生存中の参加者へメンバー個別上書き (view拒否)** を付ける。Discordの権限解決はロール層で deny集約→allow集約 の順のため、ロールへのdenyはランクロール等のallowに打ち消される。メンバー上書きはロール層より後に適用されるので制限卓でも確実に隠せる。GM・管理者権限保持者からは見える。次ゲーム開始時に前ゲームの残骸チャンネルは即時削除される
- 制限卓 (初心者/中級者/上級者) は対応ランクロール保持者にのみ表示される
- **暫定運用**: 利用人数が増えるまで、初心者・中級者・上級者カテゴリと「村長ロール説明」は全ロールの明示許可を外し、DiscordのAdministrator権限保持者だけに表示する。対応ランクロール・村長ロール・manage_guild権限だけでは閲覧できない。`ADMIN_ONLY_ROOM_IDS` と `MAYOR_INFO_ADMIN_ONLY` で再公開できる
- `#統計` は既存の `#総合` の直下に1チャンネルだけ置く。一度確定したチャンネルはIDで管理し、Bot起動時に位置・所属カテゴリ・名前を変更しない
- Bot起動時に既存チャンネルやカテゴリを並び替えず、Bot管理外の配置・名前も変更しない

## 4. 卓 (部屋) システム

| 卓 | 参加条件 | GM条件 | レート対象 |
|---|---|---|---|
| 初心者 | アイアン/ブロンズ/シルバー（当面は管理者だけに表示） | 参加可能者 | ✅ |
| 中級者 | ゴールド/プラチナ/エメラルド（当面は管理者だけに表示） | 参加可能者 | ✅ |
| 上級者 | ダイヤ/マスター/グランドマスター（当面は管理者だけに表示） | 参加可能者 | ✅ |
| 総合 | 制限なし | 制限なし | ✅ |
| ローカル固定卓 (任意) | `.env`で指定したロールまたはサーバー管理者 | `.env`で指定したユーザー (複数可) | 設定可能 |
| 村長専用村 | 村主の招待制 | 村主のみ | ❌ (履歴のみ記録) |

- 同時に参加/GM登録できるのは全卓で1つだけ
- **村長専用村**: 「村長」ロール保持者が1人1つ作成可能。村名変更・削除・招待/削除はボタンUIで操作。ロールが外れる・サーバー退出で自動削除
- **ローカル固定卓**: `WEREWOLF_LOCAL_ROOMS_JSON`の`access_role_names`とサーバー管理者だけにカテゴリ・参加受付・VC・ゲーム中チャンネルを表示する。`allowed_gm_user_ids`でGM取得者を制限する。設定なしなら追加しない
- 専用村の村名はカテゴリ名・ロール名としてそのまま使われるため、**予約名は使用不可** (固定卓名 / ランクロール名 / 「村長」 / 「総合」 / 「村長ロール説明」、および既存の同名カテゴリ・ロール)。作成・改名時に拒否される

## 5. ゲーム進行フロー

### 5.0 事前募集

`#募集` は「後で集まる」ための予約層であり、既存の `#参加受付` は「今すぐ集まる」ための即時ロビーとして維持する。募集を作成できるのは `PRIVATE_ROOM_CREATOR_ROLE_NAME`（既定「村長」）保持者で、作成時に主催者のDM受信を実際に確認する。DM不可なら、成立・警告通知を受け取れないため作成しない。開催前の募集は1人3件まで、対象は標準4卓と`recruitment_enabled=true`のローカル固定卓で、村長専用村を含めない。

- 開催日時は7日先まで。**上限は日付単位で判定する** (`_schedule_out_of_range`)。日時で比較すると、日付セレクトが提示する最終日 (今日+`RECRUITMENT_MAX_DAYS_AHEAD`) のうち「今の時刻より後」だけが弾かれ、10:00に開いた人は最終日の11:00以降を選べない、という食い違いが起きる。Discordタイムスタンプで表示し、定員13人・補欠3人は固定。主催者、配信有無、GM、備考、参加者と補欠、参加者の最低・最高ランクを表示する。作成後に変更できるのは備考だけで、その他は廃止して複製する
- 総合卓だけ参加可能ランクを複数選択できる。選択肢は表示ランク9段階と「ランク未設定」の計10個で、未選択なら制限なし。選択したランクだけを許可するホワイトリスト方式とし、初心者参加可否や数値レート制限は別に設けない。初心者・中級者・上級者卓は固定の参加条件を使い、募集ごとの追加指定はできない
- 占有区間は開催時刻から90分の半開区間 `[T, T+90分)`。同じ卓の重なる募集を作成時に拒否し、終了と次の開始が同時刻なら重複にしない。参加者は補欠を含め、占有時間が重なる別募集へ二重登録できない
- 参加時にもDM受信を確認する。13人を超えた登録は補欠になり、参加取消で枠が空くと最古の補欠を自動繰り上げして本人へDMする。13人と登録済みGMが揃った時だけ主催者へ成立DMを送る
- GMはボタンで登録・解除する。未登録のまま移行すると主催者を自動GMにするが、初心者・中級者・上級者の参加ランク条件、およびローカル固定卓の指定GM条件を移行時に再確認し、不一致なら移行を止める
- 主催者の「卓へ移行」で13人とGMを対象卓へ一括登録する。卓が進行中なら拒否し、既存ロビーに参加者またはGMがいれば明示確認後にだけリセットする。リセット時は `#参加受付` に理由を表示する。移行後はロビーの開始ボタンが有効になり、試合へ `recruitment_id` を引き継ぐ
- 10分ごとのDB確認で開催15分以内の参加者へ1回だけDMする。GM未登録なら主催者へ自動GM警告も送る。送信済み状態は再起動をまたいで保持し、起動時に開催時刻+90分を過ぎた未開催募集を自動アーカイブする
- 主催者の一括連絡は参加者・補欠・登録済みGMへDMする。1回で最大16人へDMが飛ぶため、**募集ごとに `RECRUITMENT_CONTACT_COOLDOWN_SECONDS` の間隔**を空ける (連打・誤操作で参加者のDMが埋まるのを防ぐ)。主催者だけが使う機能なので再起動をまたぐ厳密さは要らず、間隔はメモリ上で保持する。主催者がサーバーを退出した募集は自動アーカイブする。アーカイブ済みメッセージは残して操作ボタンを無効化し、`RECRUITMENT_ARCHIVE_RETENTION_DAYS`（既定30日）後にメッセージだけを削除する

**同村拒否**は `#統計` の本人専用UIで最大10人まで設定する。新しく参加する人に対して、既に募集にいる人の拒否リストだけを見る。新規参加者自身の拒否リストは見ず、設定後も既存募集へ遡及しない。参加・補欠の登録時には適用するが、補欠繰り上げ時には再確認しない。登録と上限到達は `#運営` へ表示名とIDだけで通知し、当事者へ通知し得るメンションを使わない。

`#運営` は既存の「開発」カテゴリ内に置き、Administratorだけが閲覧する。被拒否数、受付中の卓だけを対象にしたGM強制解除、募集の強制アーカイブを提供する。進行中のGM解除はゲームを廃村にするため、選択対象から除外する。

### 5.1 ロビー
- `#参加受付` の常設ボタンは3段以内。参加 / 参加取消 / GM取得 / GM放棄 / ゲーム開始 / **次村** / **GM管理** / **ルール** / ヘルプを表示し、低頻度の「参加者を除外」「受付をリセット」はGM管理の本人専用メニューへまとめる
- ゲーム中も `#参加受付` の受付パネルは末尾に残すが、全ボタンを無効化する。ゲーム中のGM操作は `#昼` の末尾に表示する
- **ルール**ボタンはゲームに必要なレギュレーション (勝利条件・役職・1日の流れ・時間・投票・夜の行動)、**ヘルプ**ボタンは遊び方 (DM/ミュート/トラブル/GMパネル) を表示する。レート/ランクの仕様は `#統計` の「ランク仕様」ボタンに分離している
- 参加時にDMテスト送信。**DMが受け取れないユーザーは参加不可** (エラー表示)
- **サーバーオーナーと管理者権限保持者はプレイヤー参加不可** (GMは可能)。オーナーはbotから編集不可 (改名/ロール403)、両者ともチャンネル権限上書きを無視して生存中に `#霊界` が見えてしまうため
- 14人目は参加不可。13人 + GM決定で「ゲーム開始」ボタンが有効化 (GMのみ押下可)
- ロビー中の退出者は参加枠/GM枠を自動解放
- **次村**: 直前のゲーム (リセット/廃村含む) の参加者とGMを一括再登録する。**押せるのは直前のゲームのGMだけ**。**通常の参加と同じくDM開放チェックを行い**、サーバー不在・参加条件を満たさない・DM不可の人はスキップして報告。GMが参加者を兼ねていた場合は**両方**復元する。抜けたい人は「参加取消」、GMは「参加者を除外」で外せる。直前メンバーの記録はスナップショットに永続化され再起動をまたぐ
- **リセット**: ゲーム中 (GMパネル) は廃村して受付へ戻す。**ロビー中 (GM管理) は参加者とGMを解除して受付を作り直す**。どちらも確認ボタンをもう一度押して初めて実行する。直前メンバーの記録は残るので「次村」で組み直せる。GM操作の結果はGMにだけephemeralで返す

### 5.2 開始処理
1. `#昼` と `#霊界` を生成 (生成失敗時はロビーへ巻き戻して通知)
2. 番号1〜13をランダム割り当て、ニックネームを「`番号.名前`」へ変更 (番号は**2桁0埋め**: `01.名前`。32字超は切り詰め)。**死亡時の死因マーカーは接尾辞** (`01.名前(処刑)`)。VC参加者一覧は表示名の辞書順に並ぶため、番号を先頭に残して**処刑・襲撃されても並び順が動かない**ようにする。マーカーが32字上限で消えると死亡が見えなくなるので、名前側を先に切り詰めてから連結する。ゲームVCにいる未参加者 (GMを除く) は「観戦者」へ変更 (**ゲーム中の途中入室でも同様**。いずれも終了時に復元)
3. 役職抽選 → 全員へ役職DM。人狼には相方を通知。**DM送信に失敗した参加者がいる場合はゲーム中断**
4. 占い師へ初日ランダム白をDM
5. 役職確認タイム (全員発言不可)。目安30秒だが**時間切れでは自動的に議論へ進まない**。`#昼` に「📩 役職を確認した」パネルを掲示し、**参加者全員が押すと議論が始まる** (押し直しで取り消せる。誰が押していないかは村へ出さず人数だけ表示)
   - 目安時間が切れると未確認者へ催促DMを送り、宣言が揃うまで待つ。**GMの強制開始ボタンは置かない** (全員がVCに揃った直後で、押せない人が出る状況は考えにくいため)。どうしても進まない場合はGMの「強制終了」で廃村する
   - 宣言は `prep_ready_ids` / `prep_confirmed` としてスナップショットへ永続化する。再起動をまたいでも押し直しは不要で、確定済みなら復元後はパネルを出さずそのまま議論へ入る

### 5.3 昼フェーズ
- **議論開始前の猶予**: 毎日、議論に入る前に生存者のミュートを解除してから **5秒** (`DISCUSSION_GRACE_TIME`) 待ち、SEを鳴らして議論を始める。ミュート解除はギルド共有バケットで人数分捌くのに数秒かかるため、「議論が始まったのにまだ喋れない」状態を防ぎ、開始タイミングを揃える
- **議論**: 初日480秒、以降毎日60秒減、下限180秒。生存者のみ `#昼` 書き込み可・VC発言可
- **ミュート整列フェーズ**: 議論終了→投票 など「全員ミュート」へ移る境目では、ミュートを流し切り、**ゲートウェイへの反映を確認してから**次のフェーズに進む (最大 `MUTE_GRACE_TIME` = 5秒)。`member.edit(mute=)` はギルド共有バケットで429になりやすく (実測で1回あたり約9秒の待ち)、挟まないと「終了と表示されたのにまだ喋れる」状態になる
  - 反映が速ければ待ち時間ゼロで通る。既に全員ミュート済み (遺言後の夜入りなど) のときはメッセージも待ちも出さない
  - 反映を確認できないまま上限に達した場合は警告ログを残して先へ進む (ここで止まるとゲームが進まなくなるため)
- **発言許可も反映を待つ**: 弁明・遺言はミュート解除の反映を確認してからタイマーを開始する。待たずに始めると、429で解除が遅れた分だけ持ち時間が削られてしまう
- **ミュート失敗の再試行**: 429のリトライを使い切って失敗したメンバーは `MUTE_RETRY_DELAY` (3秒) 後に1度だけ再試行する (成功していれば追加APIは発生しない)
- タイマーはメッセージ編集式 (30秒ごと / **残り60秒からは毎秒の秒読み**)。残り時間は見出しマークダウン (`# ⏱️`) で大きく表示。一時停止に対応。編集はチャンネル単位のレート制限 (約5回/5秒) 内に収まる
- **シーン切替SE**: 役職確認終了 / 議論開始 / 議論終了 / 投票開示 / 決戦弁明 / 遺言 / 処刑 / 夜 / 夜明け の切替時に、Botが一時的にVCへ接続して短いトーンを再生する (`config.SE_ENABLED`)。
  - **議論終了SEに一本化**: 投票開始のSEは鳴らさない。議論終了→ミュート整列 (最大5秒) →投票開始 が数秒差で連続するため、2つ鳴らすと合図として区別できない
  - **決戦弁明・遺言は話者ごと**: 候補が順番に弁明する決戦では、各候補に1回ずつ鳴らす。鳴らす位置は**発言許可 (`_grant_speaker`) の後**——ミュート解除の反映を待ってから鳴らすので「実際に話せる瞬間」の合図になり、直前の投票開示SEとギルドロック待ち (`VOICE_QUEUE_MAX_WAIT` = 1.5秒) で競合して破棄されるのも避けられる
  - **夜明けSEは夜フェーズの終端**: 朝ログ (`_morning_log`) ではなく `_night_phase` の末尾で鳴らす。朝ログまで待つと、襲撃死の改名・ロール剥奪・DMを挟んで数秒ずれる
    - 既知の割り切り: 夜明け確定 (`morning_confirmed`) の直後に再起動した場合、復元は `_night_phase` を通らず `_process_night` → `_morning_log` へ直行するため、その1回だけ夜明けSEが鳴らない。進行には影響しないため、二重再生を避ける側を優先している
    - 既知の割り切り: 襲撃で狼陣営の勝利が確定する夜でも、勝敗判定は `_night_phase` の後 (`_process_night` → `check_win`) なので夜明けSEは鳴る。「夜が終わった合図」として鳴らす設計のため抑制しない依存 (**davey** + PyNaCl + libopus) が無い環境では自動で無効。再生失敗はゲーム進行に影響しない。同一ギルドの同時再生はロックで直列化し、待機上限を超えた古い合図は後から鳴らさず破棄する
  - **davey は discord.py 2.6 以降で音声接続に必須** (DAVE E2EEプロトコル)。入っていないと接続時に `davey library needed in order to use voice` で全てのSEが鳴らなくなる。libopus はCPUアーキテクチャ (arm64 / x86_64) がPythonと一致している必要がある
- **投票**: 候補ボタンを押した後、本人専用の確認で確定する。1人1票・自分への投票不可・2回目は「投票済みです」エラー・死亡者は投票不可
  - **候補ボタンは番号順**に並べる (参加順のままだと `01, 07, 03…` と読めない並びになる)
  - 確定結果は新しいephemeralを足さず**確認メッセージをその場で書き換えて**残す (`✅ 05.名前 に投票しました。`)。「押したのに反応が分からない」を消しつつ、ephemeralの枚数もAPI回数も増やさない。保存に失敗したときだけ確認ボタンを押し直せる状態へ戻す
  - 制限時間60秒。全員投票した瞬間に結果を一括公開 (途中経過は非表示)
  - 時間切れ時は未投票者を公表し、**既投票分のみで集計** (1票もない場合は処刑なし)
  - 結果は棒グラフ (`███░░ 3票`) + **投票内訳 (誰が誰に入れたか) を公開**。どちらも**番号順**に並べる。とくに内訳を `votes` の反復順 (＝投票が届いた順) のまま出すと、誰が先に投票したかまで公開されてしまう
- **同票時**: 同票者のみで決戦。ランダム順に1人30秒の弁明 (弁明者のみVC発言可、本人かGMのボタンで短縮可) → 全生存者で再投票 → **再同票はランダム処刑**、0票はランダム処刑
- **遺言タイム**: 投票結果の開示で処刑が確定した後、処刑処理の前に**30秒の遺言時間** (処刑確定者のみVC発言可、本人かGMのボタンで短縮可)。襲撃死には遺言はない
- **処刑**: 役職は全体非公開。「番号.名前(処刑)」へ改名。霊媒師にのみ結果DM

### 5.4 夜フェーズ
- **初日80秒、2日目以降は60秒固定**。ただしこれは**目安時間**であり、時間切れでは朝にならない。`#昼` は全員ロック
- **朝を迎えるのは全員の宣言**: 夜の間、`#昼` へ「🌅 朝を迎える」パネルを**1枚だけ**掲示し、**生存者全員が押した時点で朝**になる (目安時間内でも切り上げる)。押し直しで取り消せる。**離席したい人は押さずに待たせればよく、これが一時停止の代わりになる**
  - 夜は `#昼` の書き込みが止まっているが、**ボタン押下は送信権限とは無関係**なので押せる (役職確認タイムのパネルが同じ条件で動いている)。死亡者・観戦者にもボタンは見えるので、`defer` より前に生存参加者かを検査して無駄なAPIを使わない
  - パネルは役職DMを配り終えてから出す (先に出すと役職DMが届く前に夜が終わりうる)
  - **夜の間、宣言人数は誰にも公開しない。** 13人固定構成で夜に行動があるのは狼3・占い師・狩人の**5人だけ**なので、人数を常時公開すると「まだ押していない役職者が何人いるか」が毎晩測れ、占い師・狩人の生存がCO前に推定できてしまう。人狼は互いを知っていて夜のDM中継で相談できるため、押す順番を申し合わせてカウンタを能動的に読む道具にもなる
  - **押した本人にも人数は返さない。** 押せば見えるなら、押して確かめて押し直すだけで進捗を測れてしまい、非公開にする意味が薄れる。本人へは「宣言しました」「取り消しました」だけをephemeralで返す
  - 人数を出すのは**目安時間が切れたとき、パネル本文の書き換えで1回だけ**。全員が同時に知るので不公平が生まれない。誰が押したか・誰が押していないかは最後まで出さない (進捗はGMメニューだけが見られる)
  - 通知を新規メッセージで送らずパネル本文を編集するのは、パネルが上へ流れて見失うのを防ぐため。押下時のパネル編集はしない (夜のタイマーが残り60秒から毎秒 `#昼` を編集しており、チャンネルのレート制限バケットを奪い合うため)
  - DMを13通配っていた頃と比べ、送信APIは1晩13→1 (5夜で55→5)。減るのはグローバルのレート制限枠を使う通常送信で、押下時の `defer`/`followup` は interaction 専用ルート
  - 1枚になったことで、夜明け時にボタンを無効化して View を停止できる (DM時代は前夜のパネルが各自のDMに残るため停止できなかった)
  - パネルのメッセージIDはスナップショットへ永続化し、**次の掲示より前に古いパネルを削除する**。Viewはプロセスをまたいで復元されない (ボタンに `custom_id` が無く永続View化できない) ため、再起動後に残ったパネルは押しても「インタラクションに失敗しました」しか出ない。`get_partial_message` を使うのでメッセージ取得なしの1APIで消せる
  - 未行動の役職が押した場合は**1度だけ警告**し、もう一度押すと未行動のまま確定する (誤タップ防止)
  - **GMのみ「朝」ボタン** (強制で夜を明ける) が使える (戻らない人がいてもゲームが止まらないための逃げ道)。**置き場所は `#昼` 末尾の GMコントロールパネルだけ**
  - 一時停止中はGMの強制を受け付けない (再開直後に朝が流れて誰も追えないため)。参加者の宣言は停止中も受け付ける
  - 夜明けが確定した後は取り消し・二重強制を受け付けない (表示が巻き戻る/告知が重複するのを防ぐ)
  - 生存者以外 (死亡者・観戦者) は押せない。GMが除外した人の宣言は待たない
  - **宣言がまだ必要な人に1通も届いていない場合だけ自動で夜を明ける** (誰も朝にできず永久に止まるのを防ぐ)。目安時間切れの時点で未達の人へ再送を試み、それでも届かない相手がいれば `#昼` に名前を出してGMの「朝」で進行できるようにする
  - **夜明け後にDMを編集して無効化はしない** (人数分のAPIになるため)。**Viewも停止しない**: `View.stop()` すると discord.py は `_dispatch_item` で `None` を返してコールバックを起動しなくなり、古いパネルを押した人にはDiscordの「インタラクションに失敗しました」しか出せない。DMには前夜以前のパネルが残って誤タップしやすいので、Viewは生かしたまま `night_generation` で弾いて「この夜のパネルは終了しています」を本人へ返す。実体はゲーム終了時の `_stop_all_game_views` でまとめて停止する (夜の役職UIは `#昼`/DMに残っても押し直す動機が薄いため従来どおり `_stop_night_views` で停止する)
- **夜アクションは実行確認つき・確定後は変更不可** (誤タップ防止)。セレクトで選ぶと確認UIが出て、「実行する」で確定する
  - 「やめる」を押した場合は**元のセレクトを作り直す**。Discordのセレクトは既に選択済みの項目を選び直しても操作が飛ばないため、作り直さないと同じ相手を選べなくなる
- **人狼**: 各狼のDMに襲撃UI (セレクトのみ・確認なし・夜の間は変更可)。複数狼は**最後に選択された対象**で確定 (上書き)。**DMのUI本文に「現在の襲撃先」を常時表示**し、変更のたびに全狼のUIを更新 + 他の狼へ通知する (同じ相手を選び直した場合は通知しない)。**「噛みなし」も選択可**。仲間の狼は対象外。狼がDMに送ったメッセージは他の生存狼へ自動中継
  - **中継は夜の制限時間で打ち切る** (`wolf_relay_window_open`)。「朝を迎える」の宣言状況では開閉しない。宣言待ちで夜が延びている間も相談できると制限時間の意味が無くなるため、ゲートを `night_actions_open()` から分けている
  - **他の狼へ届くものは自由文の中継と同じ窓で閉じる**。襲撃先を変えたときの「◯◯が襲撃先を●●に変更しました」通知と、全狼のDM UIの「現在の襲撃先」更新も `wolf_relay_open()` を通す。ここを開けたままにすると、**襲撃先を選び直すだけで合図を送れて**中継打ち切りが有名無実になる
  - **襲撃・占い・護衛の選択は夜明けまで受け付けたまま**にする (未行動者へ警告DMを送って選ばせる設計のため)。他の狼へ伝わる分だけが先に閉じる
  - 制限時間後に襲撃先を変えた本人には「この変更は他の人狼へ通知されません」をエフェメラルで返す (他の狼の表示が古いまま食い違うため、黙って進めない)
  - 中継窓は非永続。復元時は同じ夜のUIを制限時間ごと出し直すため、フラグも作り直す (再起動を挟むと、その夜の中継窓はもう一度開く)
  - **中継本文は `DISCORD_MESSAGE_LIMIT` (2000字) にプレフィックス込みで収める**。「🐺 **番号.名前**: 」は表示名が最大35字なので43字を占め、上限いっぱいの本文をそのまま前置すると2043字になって送信自体が `HTTPException` (50035) で失敗する。中継の送信失敗は握り潰す実装なので、詰めないと相談が黙って仲間に届かなくなる
- **占い師**: DMのセレクトUI → 実行確認 → **確定と同時に結果を即時開示**。1晩1回で変更不可 (変更可のままだと即時開示で複数人占えてしまうため)。**その夜に襲撃されても結果は手元に残る** (占い自体は成立しているため)
  - 結果は**確認UI (エフェメラル) と通常DMの両方**へ出す。エフェメラルは閉じるかクライアント再読み込みで消えるが、`seer_target` は確定済みで占い直せないため、DMが無いとその夜の占いを失う。霊媒結果・初日白と同じく通常DMを正本にする
- **狩人**: DMのセレクトUI → 実行確認 → 確定 (1晩1回・変更不可)。自分護衛不可・連続同一護衛不可
- **一時停止中も夜の操作は受け付ける** (占い・護衛・襲撃・朝の宣言)。止まっている間に選べないと、再開後に「選んだはずの行動が消えた」ように見えるため
- **未行動者への警告**: 目安時間が切れた時点で未行動の役職がいる場合、対象者へ**警告DM**を送る。以降は全員の宣言 (かGMの強制) を待つ。未行動のまま朝を迎えた場合は「行動なし」で続行する (廃村にはしない)
- 村には「⏳ 夜の目安時間が終了しました」とだけ表示し、**誰の何が未行動かは出さない** (役職の生存状況が漏れるため)

### 5.5 朝のログ
2日目以降の議論開始前に表示: `☀️ 処刑: ○○ (役職は非公開)` / `🌙 襲撃: ○○ (死亡)` / `🕊️ 平和な朝を迎えました` / 現在の生存者数。
護衛成功・噛みなし・襲撃未行動はすべて `平和な朝` に統一する (理由を出し分けると護衛の生存状況などが漏れるため)

### 5.6 終了処理
1. **進行ログの掲示**: その村の全行動を発生順に `#昼` へ貼る。この時点で既に `GAME_OVER` へ移り全UIを停止済みで、直後に役職を全公開するため新たな情報漏洩はない
2. 勝利陣営の発表 + **全員の役職を公開** (進行ログの下に置き、結果とレート変動が長いログで押し流されないようにする)
   - 記録対象: 占い / 護衛 / 襲撃先 / 投票 / 決戦投票 / 処刑・襲撃死・除外 / 護衛成功
   - **拒否された操作も残す** (確定済みの占いへの再操作 → `占い(拒否)`)。「役職が2回実行されたのでは」という疑いをその場で晴らすのが目的で、ガードが効いた証跡になる
   - 2000字を超える場合はテキストファイルとして添付する
   - 進行ログは調査用の付加機能なので、**掲示の失敗はどんな例外でも握り潰す**。ここで例外を通すと `_end_game` ごと落ちて廃村になる
   - 記録は `state.action_log` に積み、スナップショットへ永続化する (再起動をまたいでも失わない)
3. ゲーム履歴・プレイヤー死亡情報・試合詳細統計・GMを未精算キューから**1トランザクションで保存**。レート対象卓は同じトランザクションでレートも更新し、ランクロール再同期 (対象は参加者+ランクが変わった人のみ) → 変動一覧とランク昇降格を表示
   - 詳細統計は進行ログから追加Discord APIなしで集計する。付加統計だけの保存失敗はログへ残し、勝敗・参加履歴・レートの精算を止めない
   - 試合時表示ランクは精算直前の相対ランク (`before_rank_map`) をそのまま保存する。固定レート閾値 `RANK_TIERS` は統計に使わない
   - 参加者の試合時ランク中央値 `rank_bucket` も保存するが、将来用の記録に留め、現行UIには表示しない。偶数時は下位側を採用する
4. レート対象卓では終了後推薦の対象者へDMを送り、3分以内の確定票を匿名集計して1票につきレート+1を反映
5. ニックネーム復元・一時ロール/VC個別権限の撤去
6. `#昼` と `#霊界` は300秒後に自動削除。ロビーを再投稿して次ゲーム受付

## 6. 発言・ミュート制御

**接続中メンバーの発言制御はサーバーミュート (`member.edit(mute=)`) で行う。**
チャンネル権限の `speak` 変更は**接続済みメンバーには反映されない** (再接続時に初めて再計算される) というDiscord仕様のため、フェーズ切替のミュート/解除はサーバーミュートでしか実現できない (旧チャンネル権限トグル方式は「ゲーム開始してもミュートされない」「議論終了してもミュートされない」の原因だった)。

| 対象 | 制御方法 |
|---|---|
| 生存者 (接続中) | フェーズ切替時に `_sync_server_mutes` で一括ミュート/解除。既に目標状態の人はAPIを呼ばない |
| 生存者 (これから接続) | 一時ロール「人狼進行中:卓ID」にVCの `view/connect/speak` 許可 (固定・トグルしない)。再入室を常に保証し、入室時に `on_voice_state_update` がフェーズに合わせてミュート同期 |
| 観戦者・死亡者 (これから接続) | ゲーム中はVCの `@everyone` を `speak=False` + `send_messages=False` (接続時点で発言不可 = suppress、VCのテキストチャットも書き込み不可。ゲーム終了時に解除)。**途中入室の観戦者には改名と同じPATCHでサーバーミュートも適用**する (suppressが効かない環境の保険) |
| 死亡者 (接続中) | 死亡時のニックネーム変更と同一PATCHで `mute=True` (発言可能フェーズでの死亡に対応) |
| GM | **参加者を兼ねていても自動ミュートの対象外** (全フェーズ・死亡時も含む)。VCに個別 `speak=True` も付与する |
| 弁明者 | 弁明中だけサーバーミュート解除、終了で再ミュート |

**VCの内蔵テキストチャットも塞ぐ**: ゲーム中はVCの `@everyone` を `speak=False` と**同時に `send_messages=False`** にする。音声だけ塞いでも、Discordのボイスチャンネルはテキストチャット欄を持つため、サーバーミュートされた死亡者・観戦者がそこへ書き込め、それが生存者全員に見えてしまう (霊界で知った役職をそのまま伝えられる)。昼の会話は `#昼` で行うので、VCのテキストは生存者にも開けない。終了時は両方 `None` (未設定) へ戻す。
スレッド系の権限は触らない: VCの内蔵チャットはスレッドを作れず、受け付けられない権限ビットで `set_permissions` 全体が失敗すると `speak` の制限まで巻き添えで外れる。

**GMを自動制御から外している理由**: Discordのサーバーミュートは**本人では解除できない** (Mute Members権限を持っていても解除不可。仕様として権限をバイパスする)。そのため進行役が不意に発言不能になると復旧手段がなく進行が止まる。GMは自動制御の対象外とし、**発言してはいけない時間 (夜など) は自分でミュートする運用**に委ねる。
**トレードオフ**: 参加者を兼ねるGMは夜でも技術的には発言できてしまうため、公平性はGMの自己管理に依存する。

**残留ミュート事故対策**: botがミュートした相手は `bot_muted_ids` に記録しスナップショットへ永続化する。加えて、卓ごとの無権限ロール `人狼Botミュート:{room_id}` をDiscord側の所有マーカーとして使う。**mute時は `mute=True` + マーカー付与、unmute時は `mute=False` + マーカー除去を必ず同一 `Member.edit` PATCHで送る。** これによりDiscord API成功後・DB checkpoint前にプロセスが落ちても、再起動時にマーカーを正としてBot所有か手動ミュートかを一意に照合できる。旧snapshotは新方式を有効化する前に所有記録全員へマーカーを付けて移行する。

ゲーム終了時、ギルド内のVCに接続中ならニックネーム復元と同一PATCHでmuteと自卓マーカーを即解除する。未接続・メンバー不在なら `pending_unmutes` に退避し、VC入室時にGameCogがmuteと全人狼Botマーカーを同一PATCHで回収する。起動時の非active卓cleanupは**同室VCではなくギルド全メンバー**を照合し、通常の別VCへ移動済みなら即解除、未接続/不在/進行中の別卓VCだけをpendingへ回す。全卓のrestore中はraw snapshotのactive VC一覧で先行cleanupから保護し、restore終了後はその一覧を破棄して動的な卓状態を正とする。手動ミュート (モデレーター操作) は解除対象にしない。専用村の削除時は専用アクセスロールと一緒に当該卓のマーカーロールも同じ削除ジャーナル内で削除する。

**API負荷の抑制**: メンバー編集はギルド共有バケットのため、可能な限り1回のPATCHにまとめる。ゲーム開始時は**ニックネーム変更・初期ミュート・生存ロール付与を同一PATCH**に統合 (改名13件+ミュート13件+ロール付与13件 → 13件)。死亡・除外時は改名・mute・生存ロール剥奪、再参加時は改名・生存ロール復元、ゲーム終了時は改名・mute解除・マーカー剥奪をそれぞれ同一PATCHに統合する。ランク変更も対象ランク付与と旧ランク剥奪を1回のPATCHで原子的に行い、他ロールは維持する。フェーズ切替時は目標状態と一致するメンバーをスキップするため、通常は「議論開始で解除13件」「議論終了でミュート13件」のみ。

**制約**: ゲーム中に途中入室した観戦者は権限suppressで発言不可になるが、Discord仕様によりゲーム終了後も**VCに入り直すまで**発言できない (権限変更が接続中メンバーに反映されないため)。

**API負荷設計**: 全API呼び出しは共有 `Semaphore(5)` でスロットリング。ゲーム開始処理は `start_lock` で全卓1件ずつ直列化する。さらに開始・終了・フェーズmute同期・復元・ランクロール同期・専用村の一括権限/ロール/削除操作は、全卓共通ペーサーで約1.1秒ずつ間隔を空け、別処理が重なってもバーストさせない。メッセージ送信やDMは別バケットであり、ゲーム進行を不必要に遅くしないため従来の同時実行を維持する。開始待ちは3段階、終了cleanupと議論開始前の一括ミュート解除は進捗を表示する。429の発生は `discord.http` ロガーのWARNINGとして `logs/bot.log` で観測できる。

## 7. 例外・異常系

| ケース | 処理 |
|---|---|
| DM拒否ユーザーの参加 | 参加時のDMテストで弾く (参加不可) |
| 役職DM送信失敗 | ゲーム開始を中断し廃村 |
| 14人目の参加 | エラー表示 |
| GM不在/13人未満で開始 | 開始ボタンが無効 (押せない) |
| 役職確認タイムの時間切れ | 議論は始まらない。未確認者へ催促DMを送り、参加者全員の「役職を確認した」宣言 (かGMの強制) を待つ |
| 役職確認で誰かが押さないまま戻らない | GMが確認付きの「役職確認を締切」で未確認者がいても議論を開始できる。DM不達など開始条件そのものが壊れている場合は「強制終了」で廃村する |
| 夜の時間切れ | 朝にはならない。未行動役職へ警告DMを送り、生存者全員の「朝を迎える」宣言 (かGMの強制) を待つ。未行動のまま朝を迎えた場合はその役職をスキップして続行 |
| 夜に誰かが宣言しないまま戻らない | GMが「朝」ボタンで進行する (無期限に待つのはこの逃げ道があるため) |
| **ゲーム中のVC切断/サーバー退出 (生存者)** | **自動一時停止して復帰を待つ** (回線落ち対策で即死亡にしない)。VC再入室・サーバー再参加で復帰告知、GMの「再開」で続行。サーバー再参加時はニックネーム/生存ロール/霊界ブロックを再適用。戻らない場合はGMが「プレイヤー除外」で死亡扱いにしてから再開 (未復帰のままの再開も可) |
| **GMによるプレイヤー除外 (ゲーム中)** | **死亡扱い (役職非公開) + 即時勝敗判定** (「番号.名前(除外)」へ改名)。本人の投票済み票と**本人に入っていた票**を破棄 (票を失った人は再投票可)。弁明中の本人なら弁明を即終了 |
| ゲーム中のGM退出 | 即時廃村・全体通知 |
| 接続切れ | GMが一時停止 → 再開 (タイマーも停止する) |
| **フェーズ境界での一時停止** | 一時停止はフェーズ境界の非pausable区間 (遺言終了〜夜入り、ミュート整列中など) にも差し込まれる。その間ゲームループは止まらず、次のフェーズ関数が冒頭で `state.phase` を上書きする。**再開時にフェーズを戻すのは `state.phase` が `PAUSED` のままのときだけ**にする。無条件に `phase_before_pause` を復元すると進行位置と `state.phase` が恒久的にズレ、夜なのに `night_actions_open()` が False になって占い・護衛・朝の宣言・GMの強制夜明けまで全て拒否され、廃村以外に復帰できなくなる |
| Botクラッシュ/再起動 | スナップショットから一時停止状態で復元。GMの「再開」で、準備中は保存済みの番号・役職・初日白を保ったまま未完了DMから再開、昼系は当日の議論から再開、処刑解決済みなら夜へ進み、夜は保存済みの行動・宣言・解決済み印を使って再開する。勝敗確定後の未精算結果はゲームを進めず精算だけ再試行する。サーバー不在で復元できなかった参加者は名前つきで告知 (続行可否はGM判断)。**「不在」と判定する前に `fetch_member` でAPIへ確認する**: discord.py は起動時のメンバーchunkingが `max(5秒, 人数/10000)` で終わらなくても警告だけ出して `ready` を発火するため、キャッシュだけを見ると在籍中の参加者を投票権ごと消してしまう。`NotFound` だけが退出済みで、API障害は警告ログを残して不在扱いにする |
| ゲームループの予期せぬ例外 | エラーを通知して自動廃村 (レートは変動しない) |
| 廃村/リセット | レート・統計は一切変動しない |

## 8. レーティング・ランク・シーズン

- 初期レート **1500**、レート下限 **1000** (下限到達後は負けても下がらない。勝てば通常どおり上がる)。レート対象は標準4卓と`rated=true`のローカル固定卓 (村長専用村は履歴のみ)
- **固定プール制 (ゼロサム + 勝利ボーナス)**:
  - 狼勝ち: 狼陣営4人で **+60** を分配 / 村陣営9人で **-60** を分配
  - 村勝ち: 村陣営9人で **+90** を分配 / 狼陣営4人で **-90** を分配
  - 勝者には本体ゼロサムとは別枠で **+1** の勝利ボーナス (端数は決定的なルールで分配)
- **終了後推薦** (レート対象卓のみ):
  - 推薦権は **霊媒師 / 初日に処刑された人 / 初夜に襲撃死した人**。初夜が護衛成功・噛みなしなら襲撃死者枠はない
  - 同一人物が複数条件に当てはまっても1票。各推薦者はDMで自分以外の参加者1人を選び、3分以内に確認ボタンで確定する
  - GMもそのゲームへプレイヤーとして参加していれば推薦対象。GM専任者・観戦者は対象外
  - 1票につきレート **+1**。最大3票が同じ人へ重なる。推薦者名は非公開で、結果は受取人と合計票だけを表示
  - 本体増減・勝利ボーナス・推薦ボーナスは履歴へ別々に保存し、「自分の履歴」で内訳を表示する
- **相対ランク** (シーズン**3戦**以上のアクティブプレイヤーが母集団):
  - 上から グランドマスター(全体上位5%相当・最大13人) / マスター(グランドマスターを含むマスター帯全体で上位10%) / ダイヤ10% / エメラルド10% / プラチナ10% / ゴールド15% / シルバー15% / ブロンズ15% / アイアン15%
  - 3戦未満は**1戦目から暫定ランクを表示**: アクティブ勢のレート順位に仮スロットした位置で同じ帯割りからランクを求める (枠は消費しない・順位/上位%は非表示・GM表示はマスター止まり)。0戦とアクティブ0人時は暫定ブロンズ
- ランクに応じたDiscordロール (アイアン〜グランドマスター) を自動作成・自動付与。制限卓の表示権限もこのロールで制御
- **シーズン長は3ヶ月 (90日)**: 開始から90日経過すると `#統計` に管理者向けリマインダーが自動投稿される (12時間ごとに確認、72時間に1回まで)。リセットの実行自体は手動
- **シーズンリセット**: `/season_reset` (サーバー管理権限) で全員 `新レート = 1500 + (現レート - 1500) ÷ 2`、今季戦績をゼロに。実行前の順位は `前シーズン結果` として保存。レート対象卓の進行中・終了後推薦の受付/集計中は実行不可。**実行直前にDBを自動バックアップ** (失敗時はリセット中止)
- v0.25初回起動時は、起動時バックアップを作成してから既存の現在レート・最高レート・全変動履歴・シーズンスナップショットを一律 **+300** し、1200基準から1500基準へ移行する。増減量と順位関係は変わらず、移行は一度だけ実行する
- ピークレートは通算で保持 (リセットでは消えない)

## 9. 統計 (`#統計` のボタンUI)

| ボタン | 内容 |
|---|---|
| 自分の統計 | 現在レート/ランク/順位/上位%/今季・通算成績/シーズン・役職・陣営別勝率/占い・護衛・初夜襲撃/連勝・連敗・村人連続/推薦数/前シーズン結果 |
| 全体ランキング | 今シーズンの相対ランク順位 (上位20) |
| 全体データ | 試合指標を卓別、プレイヤー指標を試合時の確定表示ランク別に表示 |
| 前シーズン | 前シーズン最終順位 |
| 最近の試合 | 直近10試合 (通し番号/卓/勝利陣営/日時はDiscordタイムスタンプ表示) |
| 自分の履歴 | 自分の直近10試合 (通し番号/役職/勝敗/レート変動) |
| ランク仕様 | レート（増減 / 終了後推薦 / 対象卓）とランク（9段階 / 決まり方 / シーズン）の2枚 |
| 同村拒否 | 本人専用表示で拒否リストを追加・解除（最大10人） |
| 不具合・改善を報告 | 不具合 / 分かりにくい / 改善要望 / その他を選び、本文と任意の発生状況を送信。Bot版・卓・フェーズ・送信チャンネルとともにSQLiteへ保存。**1人あたり直近24時間で `FEEDBACK_MAX_PER_DAY` 件まで** (誰でも押せるフォームなので、連投でDBとバックアップが膨らむのを防ぐ)。件数判定とINSERTは `BEGIN IMMEDIATE` の同一トランザクションで行い、連打で上限を越えられないようにする |
| ユーザー選択 | 指定プレイヤーの統計 |

常設UIは3段以内。報告本文は公開せず、送信者本人に報告IDだけを返す。

- **卓別の試合指標**: 村/狼勝率、平均日数、平和発生確率、初日処刑、処刑成功率、初夜噛み、朝時点の平均生存狼数、JST時間帯別試合数、GM別試合数
- **試合時ランク別のプレイヤー指標**: 占い的中率、狩人が生存していた夜の護衛成功率 (`護衛成功夜 / 狩人が生存して迎えた夜`。未行動の夜も分母)、占い師/人狼の平均生存日数、役職別勝率。`rank_at_game` を使い、試合代表値 `rank_bucket` は使わない
- 表示ランクはシーズン内の相対評価。暫定ランクはランク別表示から除外し、率・平均は `STATS_MIN_SAMPLES` (既定20) 未満なら数値を出さず「試合数不足」とする
- 詳細統計は導入後の試合だけが対象。既存試合を推測で補完せず、詳細値がない過去試合と明確に分ける

## 10. コマンド・UI一覧

**ボタンUI** (旧スラッシュコマンドを置換):
- ロビー: 参加 / 参加取消 / GM取得 / GM放棄 / ゲーム開始 / 次村 / GM管理 / ルール / ヘルプ。GM管理内に参加者除外と受付リセットを置く
- GMコントロールパネル (**ゲーム中の `#昼` の末尾に掲示**): 公開面は「GMメニュー・状況」1ボタンだけ。GMが押すと、フェーズ・日数・生存人数・投票/朝宣言の進捗・復帰待ち・API処理待ちを本人専用表示し、一時停止 / 再開 / 朝 (強制で夜を明ける) / 役職確認を締切 / 強制終了 / リセット / プレイヤー除外を操作できる。役職と夜行動の内容は表示しない (役職確認タイム中は「役職確認の宣言」の進捗も出す)
- 投票パネル (`#昼`): 候補ボタン (番号順) のみ。押すと本人専用の確認が出て、確定するとその確認メッセージが「投票しました」へ書き換わる
- 強制終了 / リセット / プレイヤー除外 / 専用村削除は確認ボタンを挟み、確認時にもGM・対象・ゲーム状態を再検証する
- **参加者を選ぶUIと参加者を並べる表示は、すべて番号順** (`models.by_number`)。投票・決戦投票・襲撃・占い・護衛・GMの除外・人狼DMの相方一覧・復帰待ち一覧などが対象。番号はランダム割り当てなので、参加順のまま出すと読めない並びになる
  - **`state.players` の dict 自体は並べ替えない。** 番号割り当て (`_start_game`) と役職配布 (`_assign_roles`) がその反復順に依存し、スナップショットも同じ順で保存・復元される。必ずソート済みのコピーを渡す
  - 例外: 進行ログは発生順 (時系列であることが目的)、決戦の弁明順はランダム (仕様)、ロビー中は番号未割り当てなので参加順
- 役職確認タイムの `#昼`: 「📩 役職を確認した」(参加者全員)。GMの確認付き強制締切はGMコントロールパネルに置く
- 夜の `#昼`: 「🌅 朝を迎える」(1枚だけ掲示。生存者全員が押すと朝)。GMの強制夜明けはGMコントロールパネルの「朝」に集約
- **参加者が使える中断・再開申請ボタンは無い**。役職確認タイム (`#昼` のパネル) と夜 (DM) は宣言ボタンを押さないことで待たせ、昼は口頭でGMに伝えて一時停止してもらう
- GMパネルはゲーム開始時に掲示し、昼・投票・弁明・遺言・夜の主要メッセージを出した後に末尾へ再配置する。ゲーム終了後は `#昼` とともに削除される。ロビーパネルは `#参加受付` 再投稿時に末尾へ戻る
- 統計: §9のボタン群 (/stats 系)
- 募集: 作成 / 募集ヘルプ / 参加 / 参加取消 / GM登録・解除 / 卓へ移行 / 主催者メニュー（備考・複製・一括連絡・廃止）
- 運営: 被拒否数 / 受付中GM解除 / 募集の強制アーカイブ

**スラッシュコマンド** (3つのみ):
| コマンド | 権限 | 内容 |
|---|---|---|
| `/season_reset [note]` | サーバー管理 | シーズンハーフリセット |
| `/private_room_create [room_name]` | 村長ロール | 専用村作成 (ボタンUIと同等) |
| `/private_room_delete` | 村長ロール | 専用村削除 (確認後に実行) |

## 11. 技術構成

### ファイル構成
```
bot/
├── bot.py              エントリーポイント (ログ/SSL/Cogロード/コマンド同期)
├── config.py           定数・卓定義・レート/ランク設定
├── models.py           Player / GameState
├── game.py             GameCog (全卓管理・イベントdispatch・スラッシュコマンド)
├── room_runner.py      RoomRunner (1卓のゲーム進行ロジック)
├── permissions.py      RoomPermissionMixin (チャンネル権限管理)
├── views.py            全ボタン/セレクトUI・ヘルプ・embed生成
├── recruitment.py      募集UI・卓移行・通知・同村拒否・運営UI
├── database.py         SQLite (aiosqlite, WAL)。data/werewolf_stats.db
├── rating.py           レート計算・相対ランク
├── simulate_games.py   偽Discordオブジェクトでの全自動ゲームシミュレータ
├── SPEC.md             本書
├── README.md           クイックガイド (セットアップ/起動/テスト)
├── requirements.txt    依存パッケージ (discord.py==2.7.1 ほか)
├── CHANGELOG.md        プレイヤー向けの変更履歴 (そのままDiscordへ貼れる粒度)
├── scripts/            run_bot.sh / stop_bot.sh / setup_venv.sh /
│                       start_bot_detached.py / verify_runtime.py /
│                       check_se_playback.py / renumber_game_ids.py /
│                       *.applescript / run_checks.sh
├── data/               werewolf_stats.db + backups/ (gitignore対象。起動時・
│                       24時間ごと・/season_reset 前に自動バックアップ、
│                       ラベル別に10世代保持。作成時に wal_checkpoint して
│                       -wal / -shm を残さず、世代を捨てるときは本体と
│                       一緒にサイドカーも消す)
├── logs/               bot.log (5MB×3世代ローテーション) / launcher.log
└── .github/workflows/  ci.yml (GitHub push時の自動チェック)
```

### 実装上の要点
- **実行環境**: Python 3.14（3.14.6で検証）。Apple Siliconでは `/opt/homebrew` のarm64 PythonをPATH探索より優先し、Rosetta上のx86_64 Pythonを拒否する。直接依存は `requirements.txt`、推移依存を含む検証済み完全固定版は `requirements-lock.txt` で管理する。ただし `discord.py[voice]` 2.7.1 が `PyNaCl<1.6` を要求するため、PyNaClだけは互換範囲内の最新1.5.0を使う
- **API負荷対策**: Discord API呼び出しは `Semaphore(5)` で全卓共有のスロットリング。権限上書きは差分がある時だけ送信
- **押下の計測**: 全員が同時に押すボタン (朝を迎える / 役職を確認した / 投票の確定) は `views.InteractionTimer` で段階ごとの所要時間を測る。`ack` (Discordへの応答) → `lock` (卓の `action_lock` 取得) → `state` (状態更新とDB保存) → `reply` (本人への返信) の4区間を記録し、合計が `SLOW_INTERACTION_SECONDS` を超えたときだけWARNINGで出す。「押したのに反応しない」の原因が、Discordの応答なのか、他の処理が握っている卓ロック待ちなのか、DB保存なのかをログだけで切り分けるため。平常時は13人×毎晩のログを出さない
- **状態永続化**: フェーズ遷移に加え、夜の行動・朝の宣言・死亡・ミュート所有・開始処理の進捗を卓スナップショットへ保存する。読込時に破損した卓だけを隔離し、他卓の復元を継続する
- **外部副作用の再実行**: 死亡通知・権限変更はoutbox、専用村作成/改名/招待/削除は進捗journalとして保存し、Discord APIとDBの間で停止しても再調停する。通知は欠落防止を優先するため障害窓では重複する場合がある
- **レート整合性**: 勝敗結果を先に未精算キューへ保存し、ゲーム履歴とレート更新を同一トランザクションで冪等精算する。推薦も1試合・1推薦者の一意制約と集計済み状態を同一トランザクションで更新し、二重加算を防ぐ。レート計算・推薦集計・シーズンリセットは `rating_lock` で直列化する
- **DBスキーマ**: games / game_players / **game_stats** / player_ratings / rating_history / game_recommendations / season_resets / rating_snapshots / room_states / room_state_quarantine / game_settlements / pending_unmutes / private_rooms / private_room_members / **recruitments** / **recruitment_entries** / **player_blocks**
  - **試合番号の表示**: `games.game_id` はAUTOINCREMENTで欠番が出るため、UIには出さず**サーバー内の通し番号 (古い順に1から)** を `ROW_NUMBER()` で振って表示する (`database._GAME_SEQ_CTE`)。欠番の原因は開発中の検証で消費したぶん (本番DBへの書き込みガードは `34c77e5` で後から導入)。**廃村 (`force_end`) は精算しないので `games` に入らず、番号も消費しない**。精算がロールバックされた場合も、SQLiteは採番カウンタを巻き戻すので消費しない
  - **`game_id` の振り直し**: `scripts/renumber_game_ids.py` が一度きりの移行として用意されている (v0.34で本番へ適用済み: 3,69,70,71 → 1,2,3,4)。**表示は通し番号なので、振り直しても画面の見え方は変わらない**。内部IDとログを突き合わせたいときだけ使う
    - 外部キーは全て `games.game_id` を `ON UPDATE NO ACTION` で参照するため、`PRAGMA foreign_keys = OFF` を**`BEGIN` より前に**置き (トランザクション内では効かない)、全テーブルを同一トランザクションで書き換えて `foreign_key_check` を通してからコミットする
    - 新旧のIDが重なるので、一度オフセット領域へ退避してから確定値へ移す**2段構えが必須**。`game_players` と `rating_history` には `game_id` の一意制約が無く、1段で順序を誤ると無警告で行が混ざる
    - `sqlite_sequence` を更新しないと次の試合が旧採番の続きになり、欠番が即座に復活する
    - 前提条件 (Bot稼働・進行中の卓・未精算・未確定の推薦・`game_id` を持つ未知のテーブル) はスクリプトが検査して拒否する
    - 復元は**`-wal` / `-shm` を先に削除してから**本体を差し替える。残したままだとSQLiteが古いWALを再生し、エラーを出さずに移行後のデータが復活する
  - `games.gm_id`: 進行GM
  - `games.gm_id`: 進行GM
  - `games.base_room_id`: 将来の増設卓を固定卓へ正規化するキー。現在は `room_id` と同値
  - `games.recruitment_id`: 募集経由の試合だけ募集ID、直接開始はNULL
  - `game_players`: 死亡日・死因・試合時表示ランク・暫定フラグ
  - `game_stats`: 日数、平和、護衛確定回数/成功、占い結果、処刑、初夜噛み、日別生存狼数、将来用ランク中央値
  - `recruitments` / `recruitment_entries`: 卓の時間予約、募集条件、参加・補欠、GM、通知・メッセージ状態
  - `player_blocks`: ギルド単位の片方向同村拒否
  - `init_db()` が列・テーブルを自動追加する。既存行はNULLのままにして過去を推測しない
- **必要なBot権限**: チャンネル管理 / ロール管理 / ニックネーム管理 / メッセージ管理 / DM送信 / **メンバーをミュート** (サーバーミュートによる発言制御に必須)。ランクロールよりBotロールを上位に置くこと

### 起動・停止 (デスクトップの .app)
- **人狼Bot起動.app**: 所有者限定のruntimeディレクトリとファイルロックで多重起動を防止し、`scripts/start_bot_detached.py` 経由で完全デタッチ起動。Discord初期化とチャンネル復元が終わりready markerが出るまで成功表示しない
- **人狼Bot停止.app**: markerのPIDだけで信用せず、Python実行ファイル・`bot.py`の絶対パス・作業ディレクトリを照合したプロセスだけへ TERM → 5秒待ち → KILL。無関係なプロセスは停止しない
- `DISCORD_GUILD_ID` で管理対象を1サーバーに固定する。ランチャー起動と直接起動は同じruntime lockを共有する
- `setup_venv.sh` は同じ厳密なプロセス照合でBot稼働中か確認し、稼働中または確認不能ならvenvを変更しない。venv更新は停止 → セットアップ → 起動の順で行う

### テスト
- `scripts/run_checks.sh`: Apple Siliconネイティブ実行検査 + 仮想環境の全Pythonファイルのコンパイル + 依存整合 + 全ユニットテスト + シミュレータ実行
- `simulate_games.py`: 実ロジックを偽Discordオブジェクトで駆動。「役職を確認した」「朝を迎える」の実際の全員宣言ゲートも通過しなければ失敗する。精算後の詳細統計・死亡NULL・狼推移・中央値ランクも実ゲーム状態と突き合わせ、ランク取得失敗と非レート卓を専用試合で検証する。`--mode population` でレート分布の長期シミュレーションも可能
  - **本番DBガード**: シミュレーションは本物のゲームループを回すため `games` / `player_ratings` / `rating_history` へ実データと同じ形式で書き込む。`database.DB_PATH` をテンポラリへ差し替え忘れると偽の試合とレートが本番DBへ入り統計とランクが壊れる (実際に65試合・1,952行が混入した)。1試合ぶんの入口 `simulate_selected_game` で本番パスを検出したら `RuntimeError` で止める
  - 検証スクリプト・テストからシミュレーションを回すときは必ず `async with simulate_games.sandbox_db():` を通すこと (テンポラリDBの作成・`init_db`・後始末をまとめて行う)
- `.github/workflows/ci.yml`: GitHubへpushすれば同チェックがCIで走る

## 12. v2からの主な変更点

| 項目 | v2 | v3 (現行) |
|---|---|---|
| 構成 | 単一ゲーム | 標準4卓+任意のローカル固定卓+村長専用村の並行運用 |
| レート | なし | 固定プール制+相対ランク+シーズン |
| 人狼の相談 | #狼の巣チャンネル | DM自動中継 |
| 投票 | 棄権不可・全員完了待ち | 60秒制限+既投票分集計 (デッドロック回避) |
| 再同票 | 未定義 | ランダム処刑 |
| 霊媒結果 | 役職を通知 | 人狼/村人の2値 (占いと統一) |
| 夜の時間切れ | 廃村 | 未行動スキップで続行 |
| DM拒否者 | 警告のみ | 参加不可 (役職DM失敗は中断) |
| タイマー | Discordタイムスタンプ | メッセージ編集式 (一時停止対応のため) |
| コマンド | スラッシュ14個 | ボタンUI+スラッシュ3個 |
| 障害復旧 | なし | スナップショット復元+途中再開 |
| プレイヤー退出 | 未定義 | 死亡扱い+即時勝敗判定 |
| 霊界チャンネル | 設置しない | 各卓にゲーム中のみ `#霊界` を設置 (死亡者+観戦者専用、生存者には不可視) |
