# Discord 人狼Bot 仕様書

対応Bot: **v0.56** / 文書版: **12** / 策定日: 2026年8月
本書を現行仕様の基準とし、実装・README・テスト・CHANGELOGと相互に照合する。
文書版6では13人村・9人村と、クロストーク・ターン制を組み合わせた4変種を定義する。
常設卓は全廃し、村はGMの名前村（複数保有可）とローカル固定卓だけとする。
旧仕様v2からの主な変更点は[末尾](#12-v2からの主な変更点)参照。

## 1. 概要

| 項目 | 内容 |
|---|---|
| プレイヤー人数 | 13人または9人（卓の変種ごとに固定） |
| 操作方式 | ボタンUI主体 (スラッシュコマンドは管理用の3つのみ) |
| 卓 (同時開催) | GMまたは仮GMが作る名前村（1人で複数保有可）+ 任意のローカル固定卓（実運用では「ねいとくん村」）。常設卓は全廃。13人ターン制は実装のみで未公開 |
| レーティング | 13人村・9人クロストーク・9人ターン制の3ラダー。固定プール制レート + 卓帯補正 + 活躍ボーナス + 相対ランク9段階 + 共通シーズン境界 |
| ゲーム用チャンネル | `#昼` をゲームごとに自動生成。全村で結果発表300秒後に公開ログカテゴリへ退避 |
| 統計・報告 | 既存の案内チャンネル `#総合` 直下にある `#統計` (永続・ボタンUI) |
| 事前募集 | `GM/#村作成` で名前村と同時作成し、その村の `#参加受付` で参加から開始まで完結 |
| 役職通知 | DM送信 (DM受信不可のユーザーは参加不可) |
| 障害復旧 | ゲーム状態をSQLiteへ常時保存し、Bot再起動後に途中再開可能 |
| 言語 | 日本語 |

## 2. 役職と勝利条件

役職配分は変種の人数ごとに固定する。

- 13人村: **人狼3 / 狂人1 / 占い師1 / 霊媒師1 / 狩人1 / 村人6**
- 9人村: **人狼2 / 狂人1 / 占い師1 / 霊媒師1 / 狩人1 / 村人3**

役職・番号の割り当ては `secrets.SystemRandom` (暗号学的乱数) を使用。

| 役職 | 陣営 | 能力 |
|---|---|---|
| 人狼 ×3 / ×2 | 狼 | 夜にDMで相談 (自動中継) し、襲撃対象をUIで決定。役職DMで相方の名前を通知 |
| 狂人 ×1 | 狼 | 能力なし。狼が誰かは知らない。判定上は「村人」扱い |
| 占い師 ×1 | 村 | 夜に `#昼` の [占い] ボタンから1人を占い、結果「**人狼 / 村人**」の2値を確認 (即時開示。DMは送らず、[占い] からいつでも見返せる)。開始直後に初日ランダム白 (人狼と占い師自身を除く参加者から選出) が記録され、同じボタンから確認できる |
| 霊媒師 ×1 | 村 | 処刑直後、処刑者が「**人狼 / 村人**」だったかを `#昼` の [霊媒] ボタンでいつでも確認 (役職そのものは通知しない。DMでの通知は廃止) |
| 狩人 ×1 | 村 | 毎夜1人を護衛 (護衛放棄不可)。自分護衛不可・連続同一護衛不可 (選択肢から除外+エラー表示) |
| 村人 ×6 / ×3 | 村 | 能力なし |

**勝利条件** (昼処刑直後・夜襲撃直後・プレイヤー除外直後に判定):
- 村陣営: 全人狼の死亡
- 狼陣営: 生存人狼数 ≧ 生存非人狼数 (狂人は非人狼として数える)

## 3. チャンネル・カテゴリ構成

```
GM/                       ← GMまたは仮GMロール保持者だけに表示
  #村作成                (名前村+募集の作成/村名変更/削除ボタン)
#総合                    (既存の総合案内チャンネル。廃止した「総合」卓とは無関係)
#統計                    (#総合の直下。永続・書き込みはBotのみ)
<既存カテゴリ>/             (Bot管理外。配置や名前を変更しない)
開発/
  #運営                  (閲覧者はDiscordで手動設定可能。運営操作はAdministrator・所有者・設定運営ロール)
<卓名>/                  ← 卓ごとに1カテゴリ (GM名前村は公開、ローカル固定卓は設定に従う)
  #参加受付              (名前村では募集カードも兼用。参加/取消/開始/管理)
  🔊人狼ゲーム           (永続VC。ゲーム中の音声会話)
  #昼                    (ゲーム中のみ存在。議論・投票・結果発表)
  #霊界                  (ゲーム中のみ存在。死亡者+観戦者の雑談。生存者には見えない)
```

- 死亡者: `#昼` が読み取り専用になり、`#霊界` で観戦者と会話できる (死亡時にメンバー個別の閲覧ブロックが解除される)
- 観戦者 (未参加者): `#昼` は読み取りのみ、`#霊界` は書き込み可。ゲーム中はVCで発言不可 (§6)
- `#霊界` の実装: 部屋の表示権限をベースに、**生存中の参加者へメンバー個別上書き (view拒否)** を付ける。Discordの権限解決はロール層で deny集約→allow集約 の順のため、ロールへのdenyはランクロール等のallowに打ち消される。メンバー上書きはロール層より後に適用されるので、ロール制限のあるローカル固定卓でも確実に隠せる。GM・管理者権限保持者からは見える。次ゲーム開始時に前ゲームの残骸チャンネルは即時削除される
- 総合・初心者・中級者・上級者・総合-9クロストーク・総合-9ターンの旧常設卓はすべて廃止済みで、新規作成しない。無効卓に進行中snapshot・募集・Bot所有チャンネルIDが再混入した場合は自動削除せず、復旧手掛かりを守るためDiscord処理前に起動を止める
- `GM/#村作成` は `GM_INFO_ADMIN_ONLY=False` を標準とし、GMまたは仮GMロールへだけ明示的な閲覧許可を付ける。`@everyone` には非公開で、AdministratorはDiscord仕様により閲覧できる
- 旧 `#募集` は新規作成・自動採用・自動削除をしない。同名の手動チャンネルを含めBotは触れず、募集導線は各GM村の `#参加受付` だけを使う
- `#統計` は既存の案内チャンネル `#総合` の直下に1チャンネルだけ置く (このチャンネルは廃止した「総合」卓と無関係で、Botは作成も削除もしない)。一度確定したチャンネルはIDで管理し、Bot起動時に位置・所属カテゴリ・名前を変更しない
- Bot起動時に既存チャンネルやカテゴリを並び替えず、Bot管理外の配置・名前も変更しない

## 4. 卓 (部屋) システム

| 卓 | 参加条件 | GM条件 | レート対象 |
|---|---|---|---|
| ローカル固定卓 (任意) | `.env`で指定したロールまたは「サーバーを管理」権限 (`manage_guild`) 保持者 | `.env`で指定したユーザー (複数可) | `rated` 次第（実運用の「ねいとくん村」は対象外） |
| GMの名前村 | 公開（参加条件は募集カードで判定） | 村主のみ | ✅ |

- v0.47で常設卓「総合」を廃止し、村はGMの名前村とローカル固定卓だけになった。`open` の定義は過去試合の卓名解決のため `enabled=False` で残す（`ROOM_DEFINITIONS` は履歴用に全定義を保持する）。**それまでに積み上がった総合の統計は消さず、「全卓」集計に残る**
- **レート対象外の卓** (`rated=false`): レートを一切動かさず、勝率・ランキング・自分の統計・最近の試合のいずれにも出さない。統計の卓セレクトにも並べない。試合記録自体は残すので、ログチャンネルの試合番号は連番のまま欠けない。コード側の既定は `config.py` の `unrated_room_names`、`.env` の `rated` を書けばそちらが優先される

- 待機中の村への参加/GM登録は複数可能。進行中のゲームだけは、参加者・GMを問わず1人1村に制限する
- **GMの名前村**: GMまたは仮GMロール保持者が作成可能。**同時保有数は 仮GM 1個 / GM 3個 / 設定運営ロール併用 7個**（複数ロールは合算せず最大値）で、**サーバー全体では12個**まで（1村がカテゴリ1つとチャンネル2〜4本を使うため、Discordのカテゴリ50・チャンネル500上限を守る）。上限に達した状態では新規作成だけを断り、既存の村は残す（ロール降格時も同じ）。**待機中の募集への参加・GM登録は複数村で可能**だが、**同時にゲームを進行できるのは参加者・GMを問わず1人1村**までで、開始時に他の村で進行中なら断る。`GM/#村作成` で募集も同時作成し、村名変更・削除と参加受付はボタンUIで操作する。**どの村を使うかは村名で決める**: 既存の村と同じ村名なら その村で次の募集を受け付け、新しい村名なら上限の範囲で新規作成する（村名が空欄で村が1つだけならその村、複数あるときは村名の入力を求める）。削除・村名変更は、村が複数あるときセレクトで対象を選ぶ。カテゴリ・VC・`#昼`・`#霊界`は公開観戦型で、未参加者も閲覧・VC接続できる（生存参加者だけはゲーム中の `#霊界` を閲覧不可）。両ロールが外れる・サーバー退出で自動削除する
- 名前村のカテゴリは **`GM` カテゴリの直下** (`position = GMカテゴリ + 1`) に作る。positionを渡さないとDiscordはサーバー最下部へ足すため、作ったGM自身が一番下まで辿らないと自分の村を見つけられない。並びは**作成順 (新しい村ほど上)** とし、開始時刻順にはしない——時刻順は募集の時刻変更や試合ごとに既存カテゴリの並べ替えPATCHが要るのに対し、作成順は作成時の1パラメータで済み、既存カテゴリのpositionを一切触らないため。GMカテゴリを特定できない場合と固定卓は従来どおり最下部へ作る
- **GM／仮GM**: Bot上の名前村・募集の権限は同じ。Discordの表示順だけGMを仮GMより上に置く。「管理者村」ロールは作成・参加・GM操作の条件に使わない
- **ローカル固定卓**: `WEREWOLF_LOCAL_ROOMS_JSON`の`access_role_names`で参加資格、`allowed_gm_user_ids`でGM取得者を制限する。`sync_permissions`は通常省略時`true`だが、このBotの「ねいとくん村」だけは省略時も`false`として扱う。`false`の卓はカテゴリ・`#参加受付`・VCをDiscordで事前作成し、平常時の権限を手動設定のまま保持する。3つのいずれかが見つからなければBotは自動作成せず起動を止める。設定なしなら卓自体を追加しない
- 手動保持の対象は平常時の静的権限だけ。ゲーム開始時の`#昼`・`#霊界`はカテゴリの手動overwriteを複製して作り、生存・死亡に応じた個別権限と書込制御、VCの`speak`・`send_messages`だけを秘密保持のためBotが一時管理する。終了時はVCの開始前値へ戻す。Bot所有IDを確認できない同名の`#昼`・`#霊界`は削除せず、安全のため起動・開始を止める
- 名前村の村名はカテゴリ名としてそのまま使われるため、**予約名は使用不可** (固定卓名 / 「GM」 / 廃止済みを含む常設卓名 / ログカテゴリ名、および既存の同名カテゴリ)。作成・改名時に拒否される

### 4.1 変種とラダー

ゲーム結果は、統計を分ける `variant_id` と、レート・ランクを共有する `ladder_id` の2階層で扱う。

| `variant_id` | 表示名 | `ladder_id` | 人数 | 議論 | 村勝ちプール / 狼勝ちプール | 狼予想枠 | 終盤判定 |
|---|---|---|---:|---|---:|---:|---:|
| `v13_cross` | 13人クロストーク | `l13` | 13 | クロストーク | 180 / 120 | 3 | 6日目 |
| `v13_turn` | 13人ターン制 | `l13` | 13 | ターン制 | 180 / 120 | 3 | 6日目 |
| `v9_cross` | 9人クロストーク | `l9_cross` | 9 | クロストーク | 90 / 110 | 2 | 4日目 |
| `v9_turn` | 9人ターン制 | `l9_turn` | 9 | ターン制 | 90 / 110 | 2 | 4日目 |

- `v13_cross` と `v13_turn` は同じ `l13` レート・ランクを共有し、統計だけを変種別に分ける。9人クロストークと9人ターン制は能力差を別評価するため、統計・レート・順位・ランク履歴をそれぞれ `l9_cross` / `l9_turn` に分ける
- `l13` / `l9_cross` / `l9_turn` はレート・ランクの計算と保存を分離する。ただしシーズンの開始・終了・リセット時点は3ラダー共通
- 13人村の既存9段階ランクロールはそのまま使う。GM到達時のロール名は、`l13` が `グランドマスター`、`l9_cross` が `グランドマスター9`、`l9_turn` が `グランドマスター9T`
- 3種類のグランドマスターロールは同時に保持できる。Discordのメンバー一覧は最上位のロール欄に1回だけ表示され、全保持ロールはプロフィールで確認できる
- 9人2形式の初期設定は同じ勝敗プール・狼予想枠・終盤判定・活躍/推薦ボーナスを使うが、ラダーは共有しない。30〜50試合たまったら変種別の均衡モニターを見て、必要ならそれぞれ `W/V = (1-p)/p` で再評価する
- 利用者が選べるのは `v13_cross` / `v9_cross` / `v9_turn` の3形式。固定卓の稼働状態から分離した `USER_VISIBLE_VARIANT_IDS` を使い、名前村の作成時と受付中に選択できる
- `v13_turn` は将来の公開とローカル検証のため、旧常設卓の定義は履歴の卓名解決・ランク帯判定・無効卓状態の再混入の安全停止のため維持するが、利用者向けUI、Runner、カテゴリ、VC、受付、募集は作らない。既存のローカル固定卓の権限・配置は変更しない

## 5. ゲーム進行フロー

### 5.0 事前募集

募集と村作成は分離しない。GMまたは仮GMロール保持者が `GM/#村作成` の「村・募集を作成」を押し、名前村と、その村の `#参加受付` に置く募集カードを一度に作成する。独立した `#募集` や固定卓への「卓移行」は使わない。作成時に主催者のDM受信を実際に確認し、DM不可なら成立・警告通知を受け取れないため作成しない。村主は作成時点でその募集のGMになる。

- 参加ランク条件は「制限なし / 初心者 / 中級者 / 上級者」のプリセット、または個別の複数ランク指定から選ぶ。初心者はランク未設定・アイアン・ブロンズ・シルバー、中級者はゴールド・プラチナ・エメラルド、上級者はダイヤ・マスター・グランドマスター
- 受付中に13人村と9人村を切り替える場合は変更先ラダーで全参加者・補欠の条件を再検証し、条件外の人がいれば自動除外せず変更を拒否する

- 開催日セレクトの先頭に **「今すぐ」** を置く。選ぶと開始時刻・分のセレクトが消え (選ばせたのに無視される状態を作らないため)、開始は `now + RECRUITMENT_IMMEDIATE_LEAD_MINUTES` (既定10分) の秒切り捨て。`_schedule_out_of_range` が「現在より後」を要求するので0分にはできず、通知ウィンドウ (15分) より内側なので作成時点から開催前DMの対象になる。10分巡回が開始時刻をわずかに越えても、開催枠内なら「開催予定時刻を迎えています」と補完する。占有区間・重複判定・定員は予約と同じ扱い
- 開催日時は7日先まで。定員は選択形式に応じて13人または9人、補欠は共通で3人。募集カードには村名・形式・定員・主催者・配信有無・備考・参加者と補欠を表示する
- 公開する形式は **13人クロストーク / 9人クロストーク / 9人ターン制** の3つ。作成時に選び、ゲーム開始前の受付中は村主が `#参加受付` の「ゲーム形式を変更」から随時変更できる。村の形式、募集カード、定員、占有時間を同時に更新し、9人を超える参加者がいる状態では9人形式へ変更しない
- 名前村のカテゴリ・`#参加受付`・VC・`#昼`・`#霊界`は公開観戦型にする。「参加」は閲覧権限を変える操作ではなく、参加意思・定員・ランク条件・同村拒否・DM受信・二重参加を確認し、募集参加記録を確定する操作とする。参加取消は募集参加記録だけを解除し、VCから切断せず観戦者としての閲覧・接続を維持する
- 定員を超えた登録は補欠になり、参加取消で枠が空くと最古の補欠を自動繰り上げして本人へDMする。定員が揃った時だけ村主へ成立DMを送る
- `#参加受付` の `🔔通知` は、押した本人の権限を変えずに通知設定パネル (`user_notification_prefs`。§10「通知設定」) を開く。うち「村ができた時に通知」が従来どおり通知専用の `@通知` ロールだけを付与・解除する項目で、新しい募集カードを公開できた時にだけ1回メンションし、編集・形式変更・再起動復元では再通知しない。送信時の `AllowedMentions` はこのロールと固定文だけを対象にし、利用者が入力した募集名・備考は通知本文へ混ぜない
- 村主の「ゲーム開始」で受付中の参加者を確定し、そのまま同じ名前村で開始する。別の固定卓やロビーへ移す操作はない。試合へ `recruitment_id` と開始時点の `variant_id` を引き継ぐ
- 10分ごとのDB確認で開催15分以内の参加者へDMする。募集×参加者の処理済み台帳と同一プロセス内の通知lockにより通常運用では1人1回とし、最初の通知後に参加した人や補欠から繰り上がった人は、その処理直後（補完処理の一時障害時は次回巡回）に受け取る。Discordの恒久的なDM拒否 (`Forbidden`) は台帳へ記録して再試行せず、一時的なHTTP失敗は開催枠内で再試行する。Discord送信とSQLiteを1トランザクションにはできないため、送信成功と台帳記録の間でDB障害・プロセス停止した場合だけは再送される可能性がある。処理済み状態は再起動をまたいで保持し、開催時刻+占有時間を過ぎた未開催募集を自動アーカイブする
- 主催者メニューから備考変更・ゲーム形式変更・参加者への一括連絡・募集終了を行える。一括連絡は参加者・補欠・GMへ送り、**募集ごとに `RECRUITMENT_CONTACT_COOLDOWN_SECONDS` の間隔**を空ける。名前村を削除すると受付中の募集と村カテゴリを削除する。試合・募集の履歴はDBに残す

**同村拒否**は `#統計` の本人専用UIで最大50人まで設定する。追加対象は名前検索に加え、Bot・本人・登録済みを除くサーバーメンバー全員を25人ずつページ表示する。解除対象も登録済み50人を25人ずつ全件表示する。新しく参加する人に対して、既に募集にいる人の拒否リストだけを見る。新規参加者自身の拒否リストは見ず、設定後も既存募集へ遡及しない。参加・補欠の登録時には適用するが、補欠繰り上げ時には再確認しない。登録と上限到達は `#運営記録`（用意できない場合は `#運営`）へ表示名とIDだけで通知し、当事者へ通知し得るメンションを使わない。

`#運営` は既存の「開発」カテゴリ内だけで探し、移動・改名・新規作成しない。「開発」カテゴリまたは `#運営` が見つからない場合もゲーム本体は継続し、運営パネルと運営通知だけを無効化してログへ警告する。`@everyone`を含む既存ロール・メンバーの個別overwriteは、allow/denyや書込可否を含めてDiscord上の手動設定どおり保持し、Botは自分自身の必要権限だけを補う。閲覧できることと運営メニューの操作権は分離し、操作はサーバー所有者、Administrator、`WEREWOLF_OPERATIONS_STAFF_ROLES` の設定運営ロール、`WEREWOLF_OPERATIONS_STAFF_USER_IDS` で名指しした運営ユーザーだけに限る (ユーザー指定はロールを付けられない相手のための枠で、閲覧overwriteはこの設定でも変更しない。数字以外の値が入っていると起動時に中止する)。Bot自身の必須権限を確認・更新できない場合はそのチャンネルを採用せず、運営パネル・報告本文・起動失敗の例外本文も送らない。被拒否数、**途中離脱 (`death_cause='除外'`) の回数一覧**、**運営データ (稼働・定着・離脱・回転・通知・レートの6面。§9-4)**、9人クロストーク／ターン制の均衡監視、受付中の卓だけを対象にしたGM強制解除、募集の強制アーカイブ、**村主を問わないGM村の強制削除**（ゲーム中の村は選択不可、最終確認あり）、**シーズンリセット**を提供する。シーズンリセットだけはメニューの操作権に加えて`/season_reset`と同じ「サーバー管理」権限を要求し、判定と最終確認は同じ入口へ通す (運営ロールだけでは実行できない)。途中離脱は参加試合数と割合も出し (「10戦中3回」と「100戦中3回」を区別するため)、離脱の多い順→試合数の少ない順に並べる。**公開ランキングには出さない** (晒し上げにしないため、運営が把握する用途に限る)。進行中のGM解除はゲームを廃村にするため、選択対象から除外する。

`#運営記録` は同村拒否の登録・上限到達と、不具合・改善の報告を1件ずつ流す記録用チャンネル。`#運営` はボタンだけの操作盤として保ち、読み返す記録はこちらへ分ける。「開発」カテゴリ内だけで探し、無い場合だけ `WEREWOLF_OPERATIONS_LOG_ROLES` のロールと Bot だけが閲覧できる形で作成する (閲覧ロールにも書き込みは許可せず、投稿はBotだけ)。ロール名が未設定・サーバーに無い・同名が複数ある場合は作成せず、記録は従来どおり `#運営` へ流す (設定漏れで記録そのものを失わないため)。既存の `#運営記録` があるときは `#運営` と同じくDiscord上の手動設定を正本とし、Botは自分の必要権限だけを補う。作成後はチャンネルIDを保存し、改名されてもIDで同じチャンネルを使い続ける。起動失敗の通知だけは `#運営` へ残す (記録先を用意できない状況こそ気づける必要があるため)。

### 5.1 ロビー
- ローカル固定卓の `#参加受付` は2段で、1段目に参加 / 参加取消 / GM取得 / GM放棄 / `🔔通知`、2段目にゲーム開始 / **次村** / **GM管理** / **ルール** / ヘルプを表示する
- 名前村では同じ場所の募集カードへ参加 / 参加取消 / ゲーム開始 / `🔔通知` を1段目、**主催者メニュー** / **ルール** / **ヘルプ** / **募集** を2段目にまとめる (1段目を参加者が使うボタンだけに揃える)。ルール・ヘルプは受付中の名前村で規定を読める唯一の導線なので、**受付が閉じたカードでも押せる**まま残す (読むだけの操作のため)。**募集**は主催者だけが押せるDM一斉通知ボタン (§10)
- 主催者メニューは備考変更 / ゲーム形式を変更 / 一括連絡 / **参加者を除外** / 募集を廃止。**参加者を除外**は受付中の登録を1件外し、本人の「参加取消」と同じ経路で補欠繰り上げと開催前DMまで通す (`remove_entry_with_promotion`)。主催者自身は選べない——外せるとGM不在の募集だけが残って誰も開始できなくなるため、畳むときは「募集を廃止」を使う。除外された人の再登録は塞がない (繰り返す相手は同村拒否で扱う)
- 名前村のロビーからは参加・参加取消・GM取得・GM放棄だけを外す。**次村は残す**——常設卓の全廃後、連戦のたびに募集を作り直して全員に押し直させるのは現実的でないため
- 名前村のゲーム形式は開始前だけ変更できる (対象と拒否条件は5.0参照)
- ゲーム中も `#参加受付` の受付パネルは末尾に残すが、全ボタンを無効化する。ゲーム中のGM操作は `#昼` の末尾に表示する
- **ルール**ボタンはゲームに必要なレギュレーション (勝利条件・役職・1日の流れ・時間・投票・夜の行動)、**ヘルプ**ボタンは遊び方 (DM/ミュート/トラブル/GMパネル) を表示する。レート/ランクの仕様は `#統計` の「ランク仕様」ボタンに分離している
- 参加時にDMテスト送信。**DMが受け取れないユーザーは参加不可** (エラー表示)
- **サーバーオーナーと管理者権限保持者はプレイヤー参加不可** (GMは可能)。オーナーはbotから編集不可 (改名/ロール403)、両者ともチャンネル権限上書きを無視して生存中に `#霊界` が見えてしまうため
- ローカル固定卓では変種の定員を超える参加は不可。名前村の募集カードでは超過分を補欠にし、定員到達後に村主が「ゲーム開始」を押せる
- ロビー中の退出者は参加枠/GM枠を自動解放
- **次村**: 通常終了または強制終了した直前ゲームの参加者とGMを一括再登録する。ゲームリセットは既に同じメンバーを受付へ戻すため押し直し不要。**名前村でも使える**。**押せるのは直前のゲームのGMだけ**。**通常の参加と同じくDM開放チェックを行い**、サーバー不在・参加条件を満たさない・DM不可の人はスキップして報告。**同村拒否も見る**——募集カードを通らない経路なので、DBの登録時制約に頼らず前回メンバー内の拒否関係を1クエリで引き、先に入っている人が拒否している相手を飛ばす (誰が誰を拒否したかは出さない)。GMが参加者を兼ねていた場合は**両方**復元する。抜けたい人は「参加取消」、GMは「参加者を除外」で外せる。直前メンバーの記録はスナップショットに永続化され再起動をまたぐ
- **議論終了 (GMパネルの「スキップ」)**: 昼の議論中は「議論終了」表示になり、押すとクロストークは残り時間を捨て、ターン制は残りの巡を飛ばして投票へ進む。同じ日に二度は効かない。投票フェーズでは従来どおり発言終了・棄権・締切として働く
- **ゲームリセット**: ゲーム中 (GMパネル) は戦績を残さず終了処理を行い、名前・VC権限・役職を戻したうえで、**同じ参加者とGMを役職なし・番号なしの受付へそのまま戻す**。押し直しや「次村」は不要。ロビー中の「受付をリセット」は別操作で、参加者を全解除し、GM村では村主GMを残して前回設定の0人募集を作り直す
- **強制終了**: 戦績を残さず終了し、**参加者だけを全解除してGMは残す**。名前村は前回のタイトル・形式・ランク条件・配信設定・備考を引き継いだ0人の募集カードを自動再掲する。固定卓もGMを残した0人受付へ戻す
- どちらも確認ボタンをもう一度押して初めて実行する。GM操作の結果はGMにだけephemeralで返す。終了・リセット・参加者除外で受付や主催パネルの更新に失敗した場合は、Discordが復旧するまでバックグラウンドで再掲し、再起動時にも回収する
- **前回設定で参加受付を再開**（名前村の主催画面）: 直近の募集からタイトル・形式・ランク条件・配信設定・備考だけを複製し、**村主GM以外の参加者・補欠・通知履歴・カードメッセージIDは引き継がない空の受付**を新規作成して、参加/取消/開始などのボタンを再掲する

### 5.2 開始処理
開始前に、**全参加者（参加者を兼ねるGMを含む）がゲームVCへ接続済みであることを確認する**。未接続の参加者が1人でもいる場合は開始を拒否し、全員の接続後にあらためて開始する。
1. `#昼` と `#霊界` を生成 (生成失敗時はロビーへ巻き戻して通知)
2. 番号1〜定員をランダム割り当て、ニックネームを「`番号.名前`」へ変更 (番号は**2桁0埋め**: `01.名前`。32字超は切り詰め)。**死亡時の死因マーカーは接尾辞** (`01.名前(処刑)`)。VC参加者一覧は表示名の辞書順に並ぶため、番号を先頭に残して**処刑・襲撃されても並び順が動かない**ようにする。マーカーが32字上限で消えると死亡が見えなくなるので、名前側を先に切り詰めてから連結する。ゲームVCにいる未参加者 (GMを除く) は「観戦者」へ変更 (**ゲーム中の途中入室でも同様**。いずれも終了時に復元)
3. 役職抽選 → 全員へ役職DM。人狼には相方を通知。**DM送信に失敗した参加者がいる場合はゲーム中断**
4. 占い師の初日ランダム白を確定・記録 (DMは送らず [占い] から確認)
5. 役職確認タイム (全員発言不可)。開始と同時に、実人狼だけが役職DMへの自由文を相互中継して挨拶・相談できる**初日内通**を開始する。内通は最初の30秒、または全員確認・GM締切の早い方で終了する。役職確認自体は時間切れでは自動的に次へ進まず、`#昼` に「📩 役職を確認した」パネルを掲示する。**参加者全員が押すと1日目へ進む** (誰が押していないかは村へ出さず人数だけ表示)
   - **一度押すと取り消せない (冪等)**。トグルにしていた頃は、スマホで反応が遅いと二度タップして本人には何も返らないまま宣言が取り消され、`12/13` のまま理由が分からず進行しなかった。役職を確認し直すのにボタンを取り消す必要はない。「朝を迎える」も同じく一方向で取り消せない
   - 目安時間が切れると未確認者へ催促DMを送り、宣言が揃うまで待つ。戻らない人がいる場合はGMが確認付きの**「役職確認を締切」** (GMコントロールパネル) で未確認者がいても1日目へ進める。DM不達など開始条件そのものが壊れている場合は「強制終了」で廃村する
   - 宣言と初日内通の完了状態はスナップショットへ永続化する。再起動をまたいでも押し直しは不要で、初日内通の30秒が残っている場合だけ再開する

### 5.3 昼フェーズ

議論方式は変種で固定し、ゲーム途中には切り替えない。通常投票の集め方だけは議論方式に合わせて分かれ (§5.3.3)、決戦弁明・決戦投票・遺言は両方式で共通とする。

#### 5.3.0 昼常設パネル (CO・役職行動)

`#昼` に1枚だけ常設する `VillagePanelView` (`views.py`) を、フェーズ遷移 (昼開始・夜開始) のたびに削除→新規送信して最下部へ移す (GMパネル再掲示の直前に行い、GMパネルが常に最下部へ残る順序を守る)。**本文はフェーズ・CO一覧・公開済み結果一覧**だけで、操作説明は載せない (ボタンのラベルで足りるものを常設本文へ書くとノイズになるため)。ボタンは2段:

```
[CO] [CO撤回] [結果を公開]
[占い] [霊媒] [狩人] [人狼予想]
```

役職行動そのもの (占い・護衛・襲撃・霊能結果) は**従来どおり夜フェーズだけ・1晩1回・確定後は変更不可**で、行動結果の反映も**即時**のまま変えていない (ゲーム性は変えていない)。変わったのは操作の置き場所と結果の伝え方だけ:

- **占い**: `#昼` の [占い] ボタンから、生存する占い師が夜フェーズ中に押す。対象選択→実行確認→確定と同時に結果が返る流れは従来どおり
- **狩人**: `#昼` の [狩人] ボタンから、生存する狩人が夜フェーズ中に押す。自分護衛不可・連続同一護衛不可・護衛放棄不可は従来どおり
- **霊媒**: `#昼` の [霊媒] ボタンから霊能者が結果を確認する。能動的な行動ではなく結果確認のため受付窓を絞らない
- **[占い] [狩人] [霊媒] は本人ならいつでも押せ、初日からの自分の記録を返す** (v0.51、§5.3.0-1)。行動できる窓 (夜・生存・未確定) は変えていない
- **人狼予想**: `#昼` の [人狼予想] ボタンから、死亡直後で未提出の本人だけが押せる。**v0.51でDM経路は廃止し、提出口はここだけにした**
- **占い師DM・狩人DM・霊能結果DM・初日白DM・人狼予想DMは廃止した** (v0.51)。Botから個人へ送るDMは役職DM (開始時) と各種通知だけになった。夜の役職行動・結果確認・人狼予想はすべて `#昼` の常設パネルへ統一する。**人狼のDM (襲撃UI・狼間の自由文中継・内通含む) だけは従来どおり維持する** — 狼間の相談は自由文中継が前提で、ephemeralでは代替できないため
- **CO**: 昼フェーズ中 (DAY_DISCUSSION / DAY_VOTE / DAY_RUNOFF_* / DAY_LAST_WILL) いつでも、生存者が [CO] から役職6択 (占い師・霊能者・狩人・村人・狂人・人狼) を選んで宣言する。既にCO中は先に [CO撤回] で撤回してから宣言し直す。撤回は行を消さず `event_type='撤回'` を追記するだけで、宣言履歴は残る (§11のDBスキーマ参照)
- **結果を公開**: 参加者は[結果を公開]から、占い・霊媒・狩人の種別、対象、判定または護衛先を選び、自己申告として公開できる。実際の役職・夜行動との照合はしない。公開済みの結果はCO一覧とともに村パネルへ表示し、**夜時間からも確認できる**。同じ内容の重複公開は増やさず、訂正時は取消と新しい公開の履歴を残す
- CO・撤回はいずれも生存者かつ昼フェーズ中でVC接続済みであることを`defer`前に判定する。死亡者・VC切断中・夜フェーズ中は拒否する

##### 5.3.0-1 役職の記録ボタン (v0.51)

[占い] [狩人] [霊媒] は、**その役職本人であれば昼夜・生死を問わず**押せる。返すのは
ephemeralな「初日から現在までの自分の記録」で、夜フェーズで未確定のときだけ同じ
メッセージへ対象選択も並べる。生死で閉じないのは、**本人が既に知っている情報しか
出さない**ため (他人の情報は1行も含まない)。行動できる条件 (夜・生存・1晩1回・
確定後は変更不可) は従来どおりで、ゲーム性は変えていない。

- **占い**: `初日白（ランダム）` + 各夜の `対象 / 人狼・村人`
- **狩人**: 各夜の護衛先のみ。**GJの有無は出さない** — 朝の公開文は護衛成功・
  噛みなし・襲撃なしをすべて「平和な朝」に統一しており (§5.5)、狩人にだけ
  「これは自分のGJだ」と教えると村に出ていない情報を本人だけが持つことになる。
  記録自体は `action='襲撃確定'` / `result='GJ'` としてDBに残し、集計には使える
- **霊媒**: 従来どおり `state.medium_results` から組み立てる (新しい順)

**初日ランダム白は `game_night_actions` へ記録するだけで、DMは送らない**
(`action='初日白'`、`event_seq=0`。v0.51でDMを廃止)。開始checkpointで対象は確定
しているので、この経路が再試行で何度走っても1行に保てるよう `(guild_id,
game_run_id, actor_id, action, night_number)` が既にあれば書かない
`record_night_action_once()` を使う。`initial_seer_result_sent` は
「DM送信済み」ではなく**「確定・記録済み」**の意味で使い続ける (snapshotの項目は
減らさない)。DM不達で初日白を受け取れない事故は、これで構造的に無くなる。

記録の正本はDB (`database.list_night_actions_for_run`) で、再起動をまたいでも残る。
DB追記が落ちていた場合に限り、その夜ぶんだけ `state.seer_target` / `state.guard_target`
から補って表示する (進行は止めない既存方針のまま、表示の欠落だけを埋める)。

#### 5.3.1 クロストーク

- **議論開始前の猶予**: 毎日、議論に入る前に生存者のミュートを解除してから **5秒** (`DISCUSSION_GRACE_TIME`) 待ち、SEを鳴らして議論を始める。ミュート解除はギルド共有バケットで人数分捌くのに数秒かかるため、「議論が始まったのにまだ喋れない」状態を防ぎ、開始タイミングを揃える
- **議論**: 13人クロストーク (`v13_cross`) は初日480秒、以降毎日60秒減、下限180秒。9人クロストーク (`v9_cross`) は各日を13人村より60秒短くし、初日420秒、以降毎日60秒減、下限120秒。生存者のみ `#昼` 書き込み可・VC発言可。13人村の数値・進行・権限は据え置き

#### 5.3.2 ターン制

- 生存者が時計回り（番号の昇順、最大番号の次は01）に1人ずつ話す。死亡者は飛ばし、GMを除く現在話者以外はVCで発言不可。GMは参加者を兼ねても手動ミュートで、説明・周知のためBotはmute/unmuteしない。`#昼` は参加者の書き込みを許可せず、議論はVCだけで行う
- 初日は同じ開始席から2巡し、1巡目は1人50秒、2巡目は1人80秒。2日目以降は1巡、1人90秒。規定の巡が終わったらクロストーク時間を挟まず投票へ移る
- **GMは「議論終了」で当日の残りの巡を打ち切れる**。発言中に押した場合はその枠も閉じる。押した日 (`day_generation`) をスナップショットへ保存するため、再起動しても同じ日の議論へは戻らず投票から再開する
- 初日の開始席は `secrets.choice` でランダムに選ぶ。2日目以降は、前夜に襲撃死者がいればその席の次、護衛成功なら襲撃対象の席、どちらもなければランダムを開始席とする。一度決めた時計回りの向きは全日共通
- COは話者パネルではなく `#昼` の村パネルで受け付ける (§5.3.0)。巡や当日1回の制限はなく、昼のあいだ生存者がいつでも役職を選んで名乗り、撤回してから別役職を名乗り直せる。発言順・ミュート・持ち時間・割り込み数には影響しない
- 現在話者本人は「発言終了（パス）」で残り時間を放棄できる。13人ターン制は1日につき卓全体で2回、9人ターン制は1回だけ、別の生存者が30秒の割り込み発言を使える。この残数は陣営や個人ごとではなく、その日の村全体で共有する
- 日数・巡・開始席・現在話者・割り込み残数をスナップショットへ保存する。CO状況は村パネル側 (`co_claims`) が試合を通して保持する。再起動後は安全のため一時停止状態で復元し、GMの再開後に保存済みの進行位置から続ける

#### 5.3.3 共通の発言・投票制御

- **ミュート整列フェーズ**: 議論終了→投票 など「全員ミュート」へ移る境目では、ミュートを流し切り、**ゲートウェイへの反映を確認してから**次のフェーズに進む (最大 `MUTE_GRACE_TIME` = 5秒)。`member.edit(mute=)` はギルド共有バケットで429になりやすく (実測で1回あたり約9秒の待ち)、挟まないと「終了と表示されたのにまだ喋れる」状態になる
  - 反映が速ければ待ち時間ゼロで通る。既に全員ミュート済み (遺言後の夜入りなど) のときはメッセージも待ちも出さない
  - 反映を確認できないまま上限に達した場合は**安全停止**し、GMが権限を直して「再開」を押すまでフェーズを進めない (`_wait_for_mute_sync_or_pause` が揃うまで停止と再同期を繰り返す)
- **発言許可も反映を待つ**: 投票発言・弁明・遺言はミュート解除の反映を確認してからタイマーを開始する。待たずに始めると、429で解除が遅れた分だけ持ち時間が削られてしまう
- **ミュート失敗の再試行**: 429のリトライを使い切って失敗したメンバーは `MUTE_RETRY_DELAY` (3秒) 後に1度だけ再試行する (成功していれば追加APIは発生しない)
- クロストーク・投票・夜のタイマーはメッセージ編集式 (**30秒ごと → 残り60秒から5秒ごと → 残り30秒から毎秒の秒読み**、`timer_should_update`)。ターン制の発言タイマーは交代数が多いため、60・30・10・5秒と終了時だけを更新する (`turn_timer_should_update`)。残り時間は見出しマークダウン (`# ⏱️`) で大きく表示し、どちらも一時停止に対応する
  - 毎秒の秒読みを残り60秒から30秒へ縮めているのは、編集回数が1ゲーム (13人・5日) で1161回に達していたため。議論・投票・夜を合わせて801回まで下げた (**360回・31%減**)。メンバー編集の統合や朝パネルのDM13→1で削った量より桁が大きい。緊張感が要る最後の30秒は毎秒のまま残す
  - Discordの相対タイムスタンプ (`<t:X:R>`) への置き換えは**採用しない**。分単位でしか更新されず (1分未満は "in a few seconds" 固定)、秒読みにならない
- **シーン切替SE**: 役職確認終了 / 議論開始 / 議論終了 / 投票発言 / 投票開示 / 決戦弁明 / 遺言 / 処刑 / 夜 / 夜明け の切替時に、Botが一時的にVCへ接続して短いトーンを再生する (`config.SE_ENABLED`)。
  - **議論終了SEに一本化**: 投票開始のSEは鳴らさない。議論終了→ミュート整列 (最大5秒) →投票開始 が数秒差で連続するため、2つ鳴らすと合図として区別できない
- **通常投票の発言枠は話者ごと** (投票発言方式のクロストークのみ): 最初の人と、列が空の待機後に入った人は開始SEを鳴らしてから発言を許可する。発言終了時にもSEを鳴らし、約2秒の切替を置いてから候補選択へ移る。投票確定を公開した後、列に続く人がいれば投票開示SEと約2秒を置いて次の人へ進み、このSEを次枠の開始合図と兼用する（次枠で開始SEを重ねて約4秒待ちにしない）。最終票は直後の集計開示SEだけを鳴らす
  - **決戦弁明・遺言は話者ごと**: 候補が順番に弁明する決戦では、各候補に1回ずつ鳴らす。鳴らす位置は通常投票と逆で**発言許可 (`_grant_speaker`) の後**——ミュート解除の反映を待ってから鳴らすので「実際に話せる瞬間」の合図になり、直前の投票開示SEとギルドロック待ち (`VOICE_QUEUE_MAX_WAIT` = 1.5秒) で競合して破棄されるのも避けられる
  - **夜明けSEは夜フェーズの終端**: 朝ログ (`_morning_log`) ではなく `_night_phase` の末尾で鳴らす。朝ログまで待つと、襲撃死の改名・ロール剥奪・DMを挟んで数秒ずれる
    - 既知の割り切り: 夜明け確定 (`morning_confirmed`) の直後に再起動した場合、復元は `_night_phase` を通らず `_process_night` → `_morning_log` へ直行するため、その1回だけ夜明けSEが鳴らない。進行には影響しないため、二重再生を避ける側を優先している
    - 既知の割り切り: 襲撃で狼陣営の勝利が確定する夜でも、勝敗判定は `_night_phase` の後 (`_process_night` → `check_win`) なので夜明けSEは鳴る。「夜が終わった合図」として鳴らす設計のため抑制しない。依存 (**davey** + PyNaCl + libopus) が欠けている場合、`SE_ENABLED = True` ではDiscord接続前に起動を停止し、`SE_ENABLED = False` の場合だけSEを使わず無音で運用する。起動後の個別の再生失敗はゲーム進行に影響しない。同一ギルドの同時再生はロックで直列化し、待機上限を超えた古い合図は後から鳴らさず破棄する
  - **davey は discord.py 2.6 以降で音声接続に必須** (DAVE E2EEプロトコル)。入っていないと接続時に `davey library needed in order to use voice` で全てのSEが鳴らなくなる。libopus はCPUアーキテクチャ (arm64 / x86_64) がPythonと一致している必要がある
- **通常投票**: **クロストークだけが投票発言方式**で、ターン制は`VOTE_TIMEOUT`秒の一斉投票 (`RoomRunner.uses_sequential_vote`で分岐)。ターン制は発言順そのものが進行の骨格のため、投票まで1人ずつにすると1日が長くなりすぎる。決戦投票はどちらも一斉、決戦弁明はどちらも1人`RUNOFF_SPEECH_TIME`秒で共通
- **投票発言 (クロストーク)**: 発言順は**「投票参加」を押した順**。押した人から1人ずつ、その人だけVCで**30秒の投票発言**ができ、本人の「発言終了」でも短縮できる。発言終了SEと約2秒の切替後に候補ボタン→本人専用確認で確定すると、公開パネルへ `A → C` を即時反映する。続く人がいる場合は投票開示SEと約2秒を挟んでから次へ進む。**候補選択には時間制限を設けず、自動棄権はしない**。1人1票・自分への投票不可・死亡者は投票不可
  - **「投票参加」はいつでも押せる**。他人の発言中に押して先に並べるので、列が途切れずに回る。押した順はそのまま公開パネルの待機列に出す
  - **列が空になったら全員ミュートのまま待機する**。締切時間は設けない。誰も押さずに進行が止まったときだけ、GMが「スキップ」で締め切って未投票を棄権にできる (`vote_closed`)
    - 締め切れるのは**列を消化しきっている間だけ**。1枠の終了処理は「`vote_slot_active`を下ろす → ミュート戻し → cursor更新」の順で、その数百msはactiveが偽でも列が残る。cursorも見ないと、GMが「今の発言を飛ばす」つもりで押しただけで並んでいた全員の投票権を捨ててしまう
    - 待機中に除外・退出で生存者集合が変わったら待機を起こし直す。最後の未押下者が抜けた場合はGMのスキップを待たず投票を終える
  - **自分の枠の中で投票先が除外された人には、末尾でもう一度枠を積む**。票だけ消えて棄権になるのを防ぐ。cursorを進めてからでないと列を組み替えられないので、除外側は印 (`vote_requeue_ids`) だけ置き、投票ループが回収して積み直す
  - 議論中の仮投票は行わない。押した順・発言終了・完了位置・現在枠・締切をsnapshotへ保存し、再起動時は列と完了済み位置を維持する。進行中だった30秒枠だけは満額でやり直す
  - 投票確定後は変更不可。2回目や自分の順番以外はエラーにする
  - **候補ボタンは番号順**に並べる (参加順のままだと `01, 07, 03…` と読めない並びになる)
  - 確定結果は新しいephemeralを足さず**確認メッセージをその場で書き換えて**残す (`✅ 05.名前 に投票しました。`)。「押したのに反応が分からない」を消しつつ、ephemeralの枚数もAPI回数も増やさない。保存に失敗したときだけ確認ボタンを押し直せる状態へ戻す
  - 全員の投票発言後、得票棒グラフ (`███░░ 3票`) と投票内訳を番号順で再掲する。1票もない場合は処刑なし
- **同票時**: 同票者のみで決戦。決戦だけは**ランダム順**に1人30秒の弁明 (弁明者のみVC発言可、本人またはGMのスキップで短縮可) → **決戦候補者を除く生存者が60秒で一斉投票** → **再同票はランダム処刑**、0票はランダム処刑
- **遺言タイム**: 投票結果の開示で処刑が確定した後、処刑処理の前に**30秒の遺言時間** (処刑確定者のみVC発言可、本人かGMのボタンで短縮可)。襲撃死には遺言はない
- **処刑**: 役職は全体非公開。「番号.名前(処刑)」へ改名。結果は霊媒師が `#昼` の [霊媒] ボタンでいつでも確認できる (DM通知は廃止)

### 5.4 夜フェーズ
- **初日80秒、2日目以降は60秒固定**。この制限時間が終わるまでは「朝を迎える」を表示せず、朝へ進めない。`#昼` は全員ロック
- **朝を迎えるのは時間終了後の全員宣言**: 制限時間終了をsnapshotへ保存してから、`#昼` へ「🌅 朝を迎える」パネルを**1枚だけ**、**0 / 生存人数**で掲示する。生存者全員が押すか、GMが強制すると朝になる。宣言は一方向で取り消せず、押下の保存後に公開パネルの人数を毎回更新する
  - 夜は `#昼` の書き込みが止まっているが、**ボタン押下は送信権限とは無関係**なので押せる (役職確認タイムのパネルが同じ条件で動いている)。死亡者・観戦者にもボタンは見えるので、`defer` より前に生存参加者かを検査して無駄なAPIを使わない
  - パネルは夜の能力DMを配り、80/60秒の制限時間が終了してから初めて出す。夜時間中は人数表示もボタンも存在しないため、夜行動の進捗や役職生存を推定する材料にならない
  - 押した本人にも現在人数をephemeralで返す。公開パネルにも同じ人数が表示されるため情報差は生じず、誰が押したか・押していないかは最後まで出さない
  - 通知を新規メッセージで送らずパネル本文を編集するのは、パネルが上へ流れて見失うのを防ぐため。夜タイマーは既に終了しているので、押下時の更新とタイマー編集が競合しない
  - 生存者全員へDMを配っていた頃と比べ、送信APIは1晩あたり最大13通または9通から、`#昼` の1通へ減る。減るのはグローバルのレート制限枠を使う通常送信で、押下時の `defer`/`followup` は interaction 専用ルート
  - 1枚になったことで、夜明け時にボタンを無効化して View を停止できる (DM時代は前夜のパネルが各自のDMに残るため停止できなかった)
  - パネルのメッセージIDはスナップショットへ永続化し、**次の掲示より前に古いパネルを削除する**。Viewはプロセスをまたいで復元されない (ボタンに `custom_id` が無く永続View化できない) ため、再起動後に残ったパネルは押しても「インタラクションに失敗しました」しか出ない。`get_partial_message` を使うのでメッセージ取得なしの1APIで消せる
  - 未行動の役職が押した場合は**1度だけ警告**し、もう一度押すと未行動のまま確定する (誤タップ防止)。ただし**狩人は護衛放棄不可**で、護衛先を確定するまで何度押しても朝宣言できない
  - **GMのみ「朝」ボタン** (強制で夜を明ける) が使える (戻らない人がいてもゲームが止まらないための逃げ道)。ただし生存狩人の護衛が未確定なら強制できず、GMは護衛確定を待つかプレイヤー除外・強制終了を選ぶ。**置き場所は `#昼` 末尾の GMコントロールパネルだけ**
  - 一時停止中はGMの強制を受け付けない (再開直後に朝が流れて誰も追えないため)。参加者の宣言は停止中も受け付ける
  - 夜明けが確定した後は取り消し・二重強制を受け付けない (表示が巻き戻る/告知が重複するのを防ぐ)
  - 生存者以外 (死亡者・観戦者) は押せない。GMが除外した人の宣言は待たない
  - **朝パネルを `#昼` に掲示できなかった場合だけ自動で夜を明ける** (誰も押せず永久に止まるのを防ぐ)。ただし生存狩人の護衛が未確定なら自動で飛ばさず安全停止する。目安時間切れの時点で掲示を1度だけ試し直し、それでも `morning_panel_message` が `None` なら、未護衛時を除いて `morning_confirmed` を保存してから夜明けする
  - **夜明け時に `_close_morning_panel` がボタンを無効化してから View を停止する。** 順序が逆だと、編集が着地するまでの数百msに押した人へDiscordの「インタラクションに失敗しました」が出る。`night_generation` ガードは、再起動をまたいで残った孤児パネル対策として残す
- **占い・護衛は実行確認つき・確定後は変更不可** (誤タップ防止)。セレクトで選ぶと確認UIが出て、「実行する」で確定する。人狼の襲撃先だけは確認なしで、夜の間は何度でも変更できる (後述)
  - 「やめる」を押した場合は**元のセレクトを作り直す**。Discordのセレクトは既に選択済みの項目を選び直しても操作が飛ばないため、作り直さないと同じ相手を選べなくなる
- **人狼**: 各狼のDMに襲撃UI (セレクトのみ・確認なし・夜の間は変更可)。複数狼は**最後に選択された対象**で確定 (上書き)。**DMのUI本文に「現在の襲撃先」を常時表示**し、変更のたびに全狼のUIを更新 + 他の狼へ通知する (同じ相手を選び直した場合は通知しない)。**「噛みなし」も選択可**。仲間の狼は対象外。狼がDMに送ったメッセージは他の生存狼へ自動中継
  - **中継は夜の制限時間で打ち切る** (`wolf_relay_window_open`)。「朝を迎える」の宣言状況では開閉しない。宣言待ちで夜が延びている間も相談できると制限時間の意味が無くなるため、ゲートを `night_actions_open()` から分けている
  - **他の狼へ届くものは自由文の中継と同じ窓で閉じる**。襲撃先を変えたときの「◯◯が襲撃先を●●に変更しました」通知と、全狼のDM UIの「現在の襲撃先」更新も `wolf_relay_open()` を通す。ここを開けたままにすると、**襲撃先を選び直すだけで合図を送れて**中継打ち切りが有名無実になる
  - **襲撃・占い・護衛の選択は夜明けまで受け付けたまま**にする (未行動者へ警告DMを送って選ばせる設計のため)。他の狼へ伝わる分だけが先に閉じる
  - 制限時間後に襲撃先を変えた本人には「この変更は他の人狼へ通知されません」をエフェメラルで返す (他の狼の表示が古いまま食い違うため、黙って進めない)
  - 中継窓は非永続。復元時は同じ夜のUIを制限時間ごと出し直すため、フラグも作り直す (再起動を挟むと、その夜の中継窓はもう一度開く)
  - **中継本文は `DISCORD_MESSAGE_LIMIT` (2000字) にプレフィックス込みで収める**。「🐺 **番号.名前**: 」は表示名が最大35字なので43字を占め、上限いっぱいの本文をそのまま前置すると2043字になって送信自体が `HTTPException` (50035) で失敗する。中継の送信失敗は握り潰す実装なので、詰めないと相談が黙って仲間に届かなくなる
  - **サレンダー**: 試合中、生存中の実人狼は役職DMの常設ボタンから一方向に同意できる。生存実人狼全員が同意すると村陣営勝利として通常の終了・レート精算を行う。途中の同意人数は人狼DM内だけへ出し、公開するのは成立時だけ。死亡・除外で生存狼集合が変わった場合も再判定し、再起動時は成立checkpointを通常進行より先に回収する。狂人は同意対象外
- **占い師**: `#昼` の [占い] ボタン→対象選択 (ephemeralセレクト) → 実行確認 → **確定と同時に結果を即時開示**。1晩1回で変更不可 (変更可のままだと即時開示で複数人占えてしまうため)。**その夜に襲撃されても結果は手元に残る** (占い自体は成立しているため)
  - 結果は**確認UI (エフェメラル) にだけ**出す。**v0.51で通常DMは廃止した** — エフェメラルは閉じると消えるが、[占い] を何度押しても初日からの結果を返すようになり (§5.3.0-1)、手元へ残すためのDMが役目を終えたため。正本はDB (`game_night_actions`)
- **狩人**: `#昼` の [狩人] ボタン→対象選択 (ephemeralセレクト) → 実行確認 → 確定 (毎夜1回・変更不可・**護衛放棄不可**) 。自分護衛不可・連続同一護衛不可。候補表示だけでなく確定時にも自己・前夜の対象・候補外・死亡者を拒否する
- **一時停止中も夜の操作は受け付ける** (占い・護衛・襲撃・朝の宣言)。止まっている間に選べないと、再開後に「選んだはずの行動が消えた」ように見えるため
- **未行動者への警告**: 目安時間が切れた時点で未行動の役職がいる場合、対象者へ**警告DM**を送る。以降は全員の宣言 (かGMの強制) を待つ。**狩人以外**は未行動のまま朝を迎えた場合も「行動なし」で続行する (廃村にはしない)。狩人は護衛先を確定するまで朝へ進めない
- 村へは朝パネルを「現在 0 / Y人」で出し、以後の押下ごとに「現在 X / Y人」へ更新する。**誰の何が未行動かは出さない**

### 5.5 朝のログ
2日目以降の議論開始前に表示: `☀️ 処刑: ○○ (役職は非公開)` / `🌙 襲撃: ○○ (死亡)` / `🕊️ 平和な朝を迎えました` / 現在の生存者数。
護衛成功・噛みなし・襲撃未行動はすべて `平和な朝` に統一し、朝本文で理由を出し分けない。ターン制では護衛成功時の襲撃対象が開始席になること自体を公開情報として扱うが、朝本文にGJなどの理由や狩人の情報を追加しない

### 5.6 終了処理
1. **進行ログの掲示**: その村の全行動を発生順に `#昼` へ貼る。この時点で既に `GAME_OVER` へ移り全UIを停止済みで、直後に役職を全公開するため新たな情報漏洩はない
2. 勝利陣営の発表 + **全員の役職を公開** (進行ログの下に置き、結果とレート変動が長いログで押し流されないようにする)
   - 記録対象: 占い / 護衛 / 襲撃先 / 投票 / 決戦投票 / 処刑・襲撃死・除外 / 護衛成功
   - **拒否された操作も残す** (確定済みの占いへの再操作 → `占い(拒否)`)。「役職が2回実行されたのでは」という疑いをその場で晴らすのが目的で、ガードが効いた証跡になる
   - 2000字を超える場合はテキストファイルとして添付する
   - 進行ログは調査用の付加機能なので、**掲示の失敗はどんな例外でも握り潰す**。ここで例外を通すと `_end_game` ごと落ちて廃村になる
   - 記録は `state.action_log` に積み、スナップショットへ永続化する (再起動をまたいでも失わない)
3. ゲーム履歴・プレイヤー死亡情報・試合詳細統計・GMを未精算キューから**1トランザクションで保存**。レート対象卓は同じトランザクションでレートも更新し、ランクロール再同期 (対象は参加者+ランクが変わった人のみ) → 変動一覧とランク昇降格を表示
   - 詳細統計は進行ログから追加Discord APIなしで集計する。付加統計だけの保存失敗はログへ残し、勝敗・参加履歴・レートの精算を止めない
   - 試合時表示ランクは精算直前の相対ランク (`before_rank_map`) をそのまま保存し、固定レート閾値は使わない
   - 参加者の試合時ランク中央値 `rank_bucket` も保存するが、将来用の記録に留め、現行UIには表示しない。偶数時は下位側を採用する
4. レート対象卓では `#昼` へ終了後投票パネルを1枚出し (DMは送らない)、3分以内の確定票を匿名集計して1票につきレート+1を反映。`#昼` の退避・`#霊界` の削除は `CHANNEL_DELETE_DELAY` (300秒) 後なので、受付中にチャンネルが消えることはない
5. ニックネーム復元・一時ロール/VC個別権限の撤去。復元に失敗した名前は次の復旧機会で再試行し、復元できていないまま次のゲームへ進まないよう保護する
6. ローカル固定卓（ロール限定卓と`sync_permissions=false`の手動権限卓を含む）・GM名前村の `#昼` は、300秒後にすべて**公開ログカテゴリ (`ログ-昼`) へ退避**。`#霊界` は**退避せず削除**する。ゲーム中はGM名前村を公開観戦型とし、ローカル固定卓は各卓の閲覧設定を維持する。終了後のログは全卓共通で公開し、ロビーを再投稿して次ゲーム受付
   - `#霊界` を残さないのは、中身が入室通知と死亡者の雑談で読み返す価値が薄く、カテゴリ上限50本ぶんのチャンネル枠と試合ごとの退避APIに見合わないため。霊界での役職バラしはペナルティ対象にしていないので、証跡として残す必要もない。`LOG_CATEGORY_SPIRIT` (`ログ-霊界`) は**予約名としてだけ残す** (サーバーに残る手動作成の同名カテゴリと固定卓・名前村名が衝突しないようにするため)。過去に退避済みのログは運営が手動で削除し、Botは触らない
   - 名前は `04-昼` のように**試合番号を先頭**へ置く (Discordはカテゴリ内を名前順に並べるため、番号が前だと自然に試合順になる)。番号は統計の表示番号と同じ通し番号
   - カテゴリは**通常メンバー全員が読めて書き込めない**（AdministratorはDiscord仕様上この制限を迂回できる）。メッセージ・リアクション・スレッド・投票・外部アプリ・Webhook・チャンネル/権限管理を明示denyにし、Botだけはログ整理に必要なチャンネル/権限管理を許可する。既存カテゴリを再利用するときも、`@everyone` と既存のロール・メンバー別上書き、権限非同期の既存ログチャンネルをこの境界へ補正し、補正できなければ使用しない。同名でも `NN-昼` 以外の子を含むカテゴリは手動カテゴリとして採用しない。新しく移すチャンネルには上書き一式を直接指定し、カテゴリ更新直後のGatewayキャッシュ待ちでも旧権限をコピーしない。ログカテゴリ名は固定卓・名前村名として予約する
   - Discordの上限がカテゴリあたり50なので、`LOG_CATEGORY_LIMIT` (50) に達したら古い順に `LOG_CATEGORY_TRIM_TO` (40) まで減らす。1つずつ消すより整理の頻度が下がる。古い順の判定はチャンネルID昇順 (Snowflakeは作成時刻を含む)
   - カテゴリを用意できない・移動に失敗した場合は**従来どおり削除**する (卓のカテゴリに残して汚さない)。精算に失敗して試合番号が取れないときも削除

## 6. 発言・ミュート制御

**接続中メンバーの発言制御はサーバーミュート (`member.edit(mute=)`) で行う。**
チャンネル権限の `speak` 変更は**接続済みメンバーには反映されない** (再接続時に初めて再計算される) というDiscord仕様のため、フェーズ切替のミュート/解除はサーバーミュートでしか実現できない (旧チャンネル権限トグル方式は「ゲーム開始してもミュートされない」「議論終了してもミュートされない」の原因だった)。

| 対象 | 制御方法 |
|---|---|
| 生存者 (接続中) | フェーズ切替時に `_sync_server_mutes` で一括ミュート/解除。既に目標状態の人はAPIを呼ばない |
| 生存者 (これから接続) | 一時ロール「人狼進行中:卓ID」にVCの `view/connect/speak` 許可 (固定・トグルしない)。再入室を常に保証し、入室時に `on_voice_state_update` がフェーズに合わせてミュート同期 |
| 観戦者・死亡者 (これから接続) | ゲーム中はVCの `@everyone` を `speak=False` + `send_messages=False` (接続時点で発言不可 = suppress、VCのテキストチャットも書き込み不可。ゲーム終了時に解除)。**途中入室の観戦者には改名と同じPATCHでサーバーミュートも適用**する (suppressが効かない環境の保険) |
| 死亡者 (接続中) | 死亡時のニックネーム変更と同一PATCHで `mute=True` (発言可能フェーズでの死亡に対応)。ただしGM兼プレイヤーの音声ミュートは死亡後も手動 |
| GM | **参加者兼任を含む全GMをBotの自動mute/unmuteから除外**し、VCの個別 `speak=True` を付与する。役職・番号・投票・死亡処理は参加者として通常どおり適用する |
| 弁明者 | 弁明中だけサーバーミュート解除、終了で再ミュート |

**VCの内蔵テキストチャットも塞ぐ**: ゲーム中はVCの `@everyone` を `speak=False` と**同時に `send_messages=False`** にする。音声だけ塞いでも、Discordのボイスチャンネルはテキストチャット欄を持つため、サーバーミュートされた死亡者・観戦者がそこへ書き込め、それが生存者全員に見えてしまう (霊界で知った役職をそのまま伝えられる)。昼の会話は `#昼` で行うので、VCのテキストは生存者にも開けない。終了時、自動管理卓は両方を`None`へ戻し、`sync_permissions=false`の手動管理卓はゲーム開始前に保存した各三値 (`True` / `False` / 未設定) へ戻す。
スレッド系の権限は触らない: VCの内蔵チャットはスレッドを作れず、受け付けられない権限ビットで `set_permissions` 全体が失敗すると `speak` の制限まで巻き添えで外れる。

**GMを自動制御から外している理由**: Discordのサーバーミュートは**本人では解除できない** (Mute Members権限を持っていても解除不可)。ルール説明や操作案内が必要な進行役をBotが発言不能にするとゲームを続けられないため、参加者兼任を含むGMは自動制御の対象外とし、**発言してはいけない時間は自分でミュートする運用**に委ねる。旧snapshotにGMのBot所有マーカーが残る場合だけ自動解除し、マーカーのない手動ミュートは保護する。

**残留ミュート事故対策**: botがミュートした相手は `bot_muted_ids` に記録しスナップショットへ永続化する。加えて、卓ごとの無権限ロール `人狼Botミュート:{room_id}` をDiscord側の所有マーカーとして使う。**mute時は `mute=True` + マーカー付与、unmute時は `mute=False` + マーカー除去を必ず同一 `Member.edit` PATCHで送る。** これによりDiscord API成功後・DB checkpoint前にプロセスが落ちても、再起動時にマーカーを正としてBot所有か手動ミュートかを一意に照合できる。進行中snapshotはマーカー方式が有効であることを必須とし、旧方式は自動変換せず隔離する。現行ゲーム中にマーカーロール自体が消失した場合は再作成し、保存済みBot所有muteへ再付与してから照合する。

ゲーム終了時、ギルド内のVCに接続中ならニックネーム復元と同一PATCHでmuteと自卓マーカーを即解除する。未接続・メンバー不在なら `pending_unmutes` に退避し、VC入室時にGameCogがmuteと全人狼Botマーカーを同一PATCHで回収する。起動時の非active卓cleanupは**同室VCではなくギルド全メンバー**を照合し、通常の別VCへ移動済みなら即解除、未接続/不在/進行中の別卓VCだけをpendingへ回す。全卓のrestore中はraw snapshotのactive VC一覧で先行cleanupから保護し、restore終了後はその一覧を破棄して動的な卓状態を正とする。手動ミュート (モデレーター操作) は解除対象にしない。名前村の削除時は当該卓のマーカーロールも削除ジャーナルに従って削除する。

**API負荷の抑制**: メンバー編集はギルド共有バケットのため、可能な限り1回のPATCHにまとめる。ゲーム開始時は**ニックネーム変更・初期ミュート・生存ロール付与を同一PATCH**に統合 (改名13件+ミュート13件+ロール付与13件 → 13件)。死亡・除外時は改名・mute・生存ロール剥奪、再参加時は改名・生存ロール復元、ゲーム終了時は改名・mute解除・マーカー剥奪をそれぞれ同一PATCHに統合する。ランク変更も対象ランク付与と旧ランク剥奪を1回のPATCHで原子的に行い、他ロールは維持する。フェーズ切替時は目標状態と一致するメンバーをスキップするため、通常は「議論開始で解除13件」「議論終了でミュート13件」のみ。

**制約**: ゲーム中に途中入室した観戦者は権限suppressで発言不可になるが、Discord仕様によりゲーム終了後も**VCに入り直すまで**発言できない (権限変更が接続中メンバーに反映されないため)。

**API負荷設計**: ラッパー経由のメンバー編集・権限変更と一部のメッセージ送信・DMは共有 `Semaphore(5)` でスロットリングする。ゲーム開始処理は `start_lock` で全卓1件ずつ直列化する。さらに開始・終了・フェーズmute同期・復元・ランクロール同期・名前村の作成/改名/削除操作は、全卓共通ペーサーで約1.1秒ずつ間隔を空け、別処理が重なってもバーストさせない。募集通知・一括連絡などの直接送信はこの `Semaphore(5)` と共通ペーサーの対象外であり、別バケットとしてゲーム進行を不必要に遅くしないため従来の同時実行を維持する。開始待ちは3段階、終了cleanupと議論開始前の一括ミュート解除は進捗を表示する。429の発生は `discord.http` ロガーのWARNINGとして `logs/bot.log` で観測できる。

## 7. 例外・異常系

| ケース | 処理 |
|---|---|
| DM拒否ユーザーの参加 | 参加時のDMテストで弾く (参加不可) |
| 役職DM送信失敗 | ゲーム開始を中断し廃村 |
| 変種の定員を超える参加 | エラー表示 |
| GM不在/変種の定員未満で開始 | 開始ボタンが無効 (押せない) |
| 参加者がゲームVCへ未接続 | 参加者を兼ねるGMも含め、全員の接続が確認できるまで開始を拒否 |
| 役職確認タイムの時間切れ | 初日内通は30秒で終了するが、役職確認は進まない。未確認者へ催促DMを送り、参加者全員の「役職を確認した」宣言 (かGMの強制) を待つ |
| 役職確認で誰かが押さないまま戻らない | GMが確認付きの「役職確認を締切」で未確認者がいても1日目へ進める。DM不達など開始条件そのものが壊れている場合は「強制終了」で廃村する |
| 夜の時間切れ | その時点で初めて「朝を迎える」を0/生存人数で掲示する。未行動役職へ警告DMを送り、生存者全員の宣言 (かGMの強制) を待つ。**狩人以外**は未行動のまま朝を迎えた場合もその役職をスキップして続行するが、狩人は護衛先の確定が必須 |
| 夜に誰かが宣言しないまま戻らない | GMが「朝」ボタンで進行する。ただし未護衛の生存狩人がいれば強制朝は拒否され、GMは護衛確定を待つかプレイヤー除外・強制終了を選ぶ |
| **ゲーム中のVC切断/サーバー退出 (生存者)** | **自動一時停止して復帰を待つ** (回線落ち対策で即死亡にしない)。VC再入室・サーバー再参加で復帰告知、GMの「再開」で続行。サーバー再参加時はニックネーム/生存ロール/霊界ブロックを再適用。戻らない場合はGMが「プレイヤー除外」で死亡扱いにしてから再開 (未復帰のままの再開も可) |
| **GMによるプレイヤー除外 (ゲーム中)** | **死亡扱い (役職非公開) + 即時勝敗判定** (「番号.名前(除外)」へ改名)。本人の投票済み票と**本人に入っていた票**を破棄 (票を失った人は再投票可)。弁明中の本人なら弁明を即終了 |
| ゲーム中のGM退出 | 即時廃村・全体通知。ただし**終了処理が走っている (`ending`) か、勝敗確定済みで精算待ち (`pending_winner`) の場合は廃村せず**、その結果の精算を続行・再試行する (確定した勝敗を廃村で上書きしない) |
| 接続切れ | 生存者の切断は**Botが自動で一時停止** (上記の行を参照)。GMの手動一時停止も可能で、どちらもタイマーは停止する (`_pausable_countdown` / `_pausable_sleep` が `pause_event` で待つ) |
| **フェーズ境界での一時停止** | 一時停止はフェーズ境界の非pausable区間 (遺言終了〜夜入り、ミュート整列中など) にも差し込まれる。その間ゲームループは止まらず、次のフェーズ関数が冒頭で `state.phase` を上書きする。**再開時にフェーズを戻すのは `state.phase` が `PAUSED` のままのときだけ**にする。無条件に `phase_before_pause` を復元すると進行位置と `state.phase` が恒久的にズレ、夜なのに `night_actions_open()` が False になって占い・護衛・朝の宣言・GMの強制夜明けまで全て拒否され、廃村以外に復帰できなくなる |
| Botクラッシュ/再起動 | スナップショットから一時停止状態で復元。GMの「再開」で、準備中は保存済みの番号・役職・初日白を保ったまま未完了DMから再開、未完了の初日内通は残り時間だけ再開、昼系は当日の議論から再開、クロストーク通常投票は保存済みの待機列・発言終了・確定済み票を保って再開、処刑解決済みなら夜へ進み、夜は保存済みの行動・朝待機開始・宣言・解決済み印を使って再開する。サレンダー成立済みまたは勝敗確定後の未精算結果はゲームを進めず精算だけ再試行する。サーバー不在で復元できなかった参加者は名前つきで告知 (続行可否はGM判断)。**「不在」と判定する前に `fetch_member` でAPIへ確認する**: discord.py は起動時のメンバーchunkingが `max(5秒, 人数/10000)` で終わらなくても警告だけ出して `ready` を発火するため、キャッシュだけを見ると在籍中の参加者を投票権ごと消してしまう。`NotFound` だけが退出済みで、API障害は警告ログを残して不在扱いにする |
| ゲームループの予期せぬ例外 | エラーを通知して自動廃村 (レートは変動しない)。ただし**永続化失敗 (`StateDurabilityError`) は廃村しない** — `_stop_for_durability_error` が既に安全停止して告知済みで、ここで廃村すると守ったスナップショットを捨ててしまうため。GMの「再開」で再試行する |
| ゲームリセット | レート・統計を変えず、役職・番号・生死を初期化して同じ参加者とGMを受付へ戻す |
| 強制終了 | レート・統計を変えず、参加者0人・現在GM維持へ戻す。名前村は同設定の募集カードを再掲 |
| GM村の強制削除 | LOBBYだけで実行可能。募集・村パネル・チャンネルを含む村自体を削除 |

## 8. レーティング・ランク・シーズン

- 初期レート **1500**、レート下限 **1000** (下限到達後は負けても下がらない。勝てば通常どおり上がる)。正常終了したGM名前村と、`rated` なローカル固定卓のゲームを対象とする。13人2変種は`l13`、9人クロストークは`l9_cross`、9人ターン制は`l9_turn`でレート・ランクを別計算する。変更前に非レートで終了した過去ゲームへは遡ってレートを付けない
- **固定プール制 (ゼロサム + 勝利ボーナス)**:
  - 13人村の狼勝ち: 狼陣営4人で **+120** を分配 / 村陣営9人で **-120** を分配 (狼 各+30 / 村 各-13〜-14)
  - 13人村の村勝ち: 村陣営9人で **+180** を分配 / 狼陣営4人で **-180** を分配 (村 各+20 / 狼 各-45)
  - 9人村の狼勝ち: 狼陣営3人で **+110** を分配 / 村陣営6人で **-110** を分配 (狼 各+36〜+37 / 村 各-18〜-19)
  - 9人村の村勝ち: 村陣営6人で **+90** を分配 / 狼陣営3人で **-90** を分配 (村 各+15 / 狼 各-30)
  - 勝者には本体ゼロサムとは別枠で **+1** の勝利ボーナス (端数は決定的なルールで分配)
  - **13人村のプール比120:180は狼勝率60%、9人村の110:90は狼勝率45%で全体EV=0** を意味する (`W·p - V·(1-p) = 0` → `p = V/(V+W)`)。倍率を一律に変えても均衡勝率は動かないので、実測後は変種ごとに比率を直す。`elo_delta` は1試合ごとに厳密なゼロサムだが、勝利・活躍・推薦ボーナスを足した総`delta`はゼロサムではない
- **卓帯補正** (プールへ掛ける係数。ゼロサムは保たれる):
  - 陣営ごとに参加者の**卓帯**の中央値を取る。帯は募集の参加条件プリセットと同じ定義を使う (5.0参照)。偶数人数は下位側を採る (`build_rank_bucket` と同じ規則)
  - 帯差1につき **±10%**。差は最大2帯なので係数は **0.8〜1.2**。格上の陣営が勝つと縮み、格下の陣営が勝つと伸びる
  - **勝者側だけでなくプール本体へ掛ける。** 勝者と敗者が同じプールから出るのでゼロサム性は崩れない。整数演算 (`pool * 係数 // 100`) なので決定的
  - 1人でも試合時ランクが取れない場合は補正しない (等倍)。片側だけ歪んだ係数が掛かるのを防ぐ
  - 帯差補正は正常終了した全村で働き、募集時の初心者・中級者・上級者プリセットは参加条件として同じ帯定義を使う
  - 暫定ランクは表示されている暫定ランクをそのまま使う (通算0戦=暫定ブロンズ=初心者帯)。1戦目だけが初心者帯で、2戦目以降はレート順位の仮スロット位置しだいで中級者帯にもなりうる
  - 精算直前の `before_rank_map` から求めるので**追加のDiscord API呼び出しはない**
- **活躍ボーナス** (レート対象卓のみ。勝敗とは別枠の**非ゼロサム加点**):
  | 条件 | 加点 |
  |---|---|
  | 処刑された**人狼**へ投票していた**村陣営** (狂人は対象外) | +2 |
  | 13人村は**6回目**、9人村は**4回目**の議論へ到達した試合の**人狼** (狂人は対象外) | +2 |
  | **狼予想提出**の的中1人につき (13人は最大3人、9人は最大2人) | +1。**初日・2日目の死亡は2倍** |
  | **狩人**の護衛成功1回につき | +1 |
  | **初夜に占い師を襲撃して殺しきった**ときの人狼 | +1 |
  - 投票の加点は**処刑を確定させた最終ラウンドの票だけ**を見る (決戦があれば決戦の票)。0票・再同票による**ランダム処刑は対象外**、狂人の処刑も対象外
  - 材料は精算キューの `bonus_payload` へ載せ、精算と同じ1トランザクションで反映する。payloadが壊れていても加点なしで勝敗とレートの精算は続行する
  - 計算は `rating.calculate_play_bonuses()` の純関数。加点は常に0以上なので、レート下限のクランプより後に足してよい
  - ボーナス計算時の**追加Discord API呼び出しはない** (投票・護衛成功・初夜噛み・人狼予想はいずれも既存の状態と進行ログから取れる)
- **人狼予想提出** (全陣営共通の非公開導線):
  - **処刑・襲撃で死亡**し、その死亡後も勝敗が未確定なら、陣営に関係なく `#霊界` の閲覧解放を最大 `WOLF_GUESS_TIMEOUT` (既定120秒) 保留する。提出は `#昼` パネルの [人狼予想] から行い、人狼だと思う相手を13人村では3人、9人村では2人選ぶ。**提出するか時間切れになった時点で霊界を開ける**
  - 保留するのは、霊界へ入ると先に死んだ人から答えを聞けてしまい提出の意味が無くなるため。`_apply_death_effect` の霊界解放だけを遅らせるので、**メンバー編集のPATCH回数は増えない**
  - **v0.51でDM経路を廃止した。** 死亡者だけへ届ける手段は個別DMしかないため、案内は**既存の死亡告知 (処刑告知・朝ログ) へ1行だけ相乗り**させる (`WOLF_GUESS_NOTICE`。新しい送信は増やさない)。文面は誰の死亡でも同一で、保留中のときだけ添える。**全陣営へ同じ導線・同じ保留時間**を適用し、公開情報や霊界へ入る時刻から死亡者の陣営を推測できないようにする
  - 選択対象は自分以外の全参加者 (**既に死亡した人も選べる**)。正解はその変種の人狼（13人村は3人、9人村は2人）のみで**狂人は正解に含まない**
  - **実際の人狼本人**も情報差を作らないため提出できるが、正解を知っているので採点対象外。人狼を知らない**狂人は採点対象**とし、村役職と同じく的中数を加点する
  - **対象外**: 死因が「除外」/ 終了時まで生存 / その死亡によって勝敗が確定する場合
  - 提出UIは `game_run_id`・本人ID・死亡イベントIDを照合し、前の試合や別の死亡の古いUIを受け付けない。DMを介さないので「DMを閉じている人が提出機会そのものを失う」ケースも無くなった (旧DM失敗時のGM通知経路も併せて削除)
  - 保留状態と死亡イベントIDは部屋スナップショットへ保存するが、**復元時は待たずに全部解放する** (再起動をまたいで待つと、その間に霊界の話を聞けてしまう)。未提出は0点。自動解除タイマーもゲーム終了時に畳む (2分以内に次村が始まると前村のタイマーが新しい保留を解いてしまうため)
- **終了後の投票** (レート対象卓のみ):
  - `#昼` に**パネルを1枚**出し、3分以内に ephemeral で受ける。**DMは送らない** (投票権者は最大13人になるため、DMだと人数分の通常送信になる)。`CHANNEL_DELETE_DELAY` は300秒なので、チャンネル退避より先に受付が終わる
  - 投票受付中は旧`#昼`を残すため、募集作成と参加受付は続けられるが、次ゲームの開始だけを受付終了まで止める
  - GM名前村も公開観戦を維持したまま投票を受け付ける
  - **勝利陣営の各人**が、手強かった**敗北陣営**の1人へ1票 (`kind='postgame'`)
  - **霊媒師 / 初日に処刑された人 / 初夜に襲撃死した人**が、参加者の1人へ1票 (`kind='recommend'`)。初夜が護衛成功・噛みなしなら襲撃死者枠はない。同一人物が複数条件に当てはまってもこの枠は1票
  - 両方に当てはまる人 (勝利陣営の霊媒師など) は**2票持つ**。そのため `game_recommendations` の主キーは `(game_id, voter_id, kind)`
  - GMもそのゲームへプレイヤーとして参加していれば対象。GM専任者・観戦者は対象外
  - 1票につきレート **+1**。**得票の上限はない**。投票者名は非公開で、結果は受取人・合計票・レート変動を表示
  - 本体増減・勝利ボーナス・活躍ボーナス・投票ボーナスは履歴へ別々に保存し、「自分の履歴」で内訳を表示する
- **相対ランク** (**通算3戦**以上のアクティブプレイヤーが母集団):
  - 変種ではなくラダーごとに母集団を作る。上から グランドマスター(全体上位5%相当、`l13`は最大13人・`l9_cross`と`l9_turn`は各最大9人) / マスター(グランドマスターを含むマスター帯全体で上位10%) / ダイヤ10% / エメラルド10% / プラチナ10% / ゴールド15% / シルバー15% / ブロンズ15% / アイアン15%
  - 3戦未満は**1戦目から暫定ランクを表示**: アクティブ勢のレート順位に仮スロットした位置で同じ帯割りからランクを求める (枠は消費しない・順位/上位%は非表示・GM表示はマスター止まり)。通算0戦とアクティブ0人時は暫定ブロンズ
  - **母集団を今季ではなく通算で数えるのは、シーズンリセットで全員を暫定ブロンズへ戻さないため。** リセットは `season_games` をゼロにするので、今季で数えるとレートを保持していても全員が母集団から外れる。レート変換 `1500 + (r - 1500) // 2` は単調**非減少**なので基本の並びを保つが、整数除算で隣接レートが同着へ潰れた組は、`season_wins` が0になった後の `player_id` タイブレークで順位・ランクが入れ替わりうる。暫定ランクもアクティブ勢への仮スロット位置が動けば変わりうる。暫定ブロンズに落ちるのは**完全な新規 (通算0戦) だけ**で、初心者卓へ入れる状態を保つ
- `l13` は既存のDiscordランクロール (アイアン〜グランドマスター) を自動同期し、制限卓の表示権限もこのランクだけで制御する。`l9_cross` / `l9_turn` はグランドマスター該当者にだけ `グランドマスター9` / `グランドマスター9T` を付与し、ほかの段階は `#統計` にだけ表示する。3種類のグランドマスターロールを同時に保持できるが、メンバー一覧では最上位ロールの欄へ1回だけ表示される
- **シーズン長は3ヶ月 (90日)**: 開始から90日経過すると `#統計` に管理者向けリマインダーが自動投稿される (12時間ごとに確認、72時間に1回まで)。リセットの実行自体は手動
- **シーズンリセット**: `/season_reset` (サーバー管理権限) と `#運営` の「シーズンリセット」ボタンで`l13` / `l9_cross` / `l9_turn`を同じ境界・同じ実行でリセットする。どちらの入口でも**実行前に本人だけの最終確認 (60秒) を挟み**、押し間違いでは実行しない。確認を出した時点のシーズン開始時刻をCASへ渡すため、確認中に別の運営がリセットしていれば中止する。各ラダー内で全員 `新レート = 1500 + (現レート - 1500) ÷ 2`、今季戦績をゼロにし、順位スナップショットはラダー別に保存する。母集団は通算戦績で維持するため**全員が暫定ブロンズへ戻ることはない**が、丸めによる同着や暫定順位の変動で個別の順位・ランクは変わりうる (上記「相対ランク」を参照)。実行不可の条件は5つ: レート対象卓が進行中 / 終了後推薦の受付・集計中 / **レート未精算の終了ゲームが残っている** / **今季の対戦記録が0件 (連続リセット不可)** / **実行中に別操作でシーズンが更新された (CASで検出)**。**実行直前にDBを自動バックアップ** (失敗時はリセット中止)
- ピークレートは通算で保持 (リセットでは消えない)

## 9. 統計 (`#統計` のボタンUI)

| ボタン | 内容 |
|---|---|
| 自分の統計 | 冒頭の変種セレクトで切り替え。選んだ変種の成績と、その変種が属するラダーの現在レート/ランク/順位/上位%/今季・通算成績/シーズン・役職・陣営別勝率/占い・護衛・初夜襲撃/連勝・連敗・村人連続/推薦数/前シーズン結果。画面内に、本人が自分を見ているときだけ **[レート推移]** (§9-2) と **[相性]** (§9-3) を置く。**「画像で見る」**でアバター・レート・得意/苦手役職TOP3・直近試合などをまとめた戦績カード画像 (PNG) を生成する (§9-1)。**このボタンは既定で非表示** (§9-1参照) |
| 全体ランキング | **変種 → 指標 → 必要なら役職**の順にセレクトする。**レート順位** (選択変種のラダーにおける今シーズンの相対ランク・上位20) / **村で初日に吊られた率** / **人狼で勝った試合の生存率** / **役職別の勝率** / **終了後投票の獲得票 (1試合あたり)** / **狼予想の的中率** |
| 全体データ | 変種セレクトを付け、選択変種の試合指標を卓別、プレイヤー指標を試合時の確定表示ランク別に表示 |
| 前シーズン | 変種セレクトと項目セレクトで切り替え。**前シーズン最終順位** (選択変種のラダー上位20) / **歴代グランドマスター** (`rating_snapshots` をラダー・シーズンごとに集計。GMが0人のシーズンは出さない)。現行ランキングとは別枠 |
| 最近の試合 | 変種で絞った直近10試合 (通し番号/卓/勝利陣営/日時はDiscordタイムスタンプ表示) |
| 自分の履歴 | 変種で絞った自分の直近10試合 (通し番号/役職/勝敗/レート変動) |
| ランク仕様 | レート（増減 / 終了後推薦 / 対象卓）とランク（9段階 / 決まり方 / シーズン）の2枚 |
| 同村拒否 | 本人専用表示で拒否リストを追加・解除（最大50人、候補・解除とも25人ずつページ表示） |
| 不具合・改善を報告 | 不具合 / 分かりにくい / 改善要望 / その他を選び、本文と任意の発生状況を送信。Bot版・卓・フェーズ・送信チャンネルとともにSQLiteへ保存。**1人あたり直近24時間で `FEEDBACK_MAX_PER_DAY` 件まで** (誰でも押せるフォームなので、連投でDBとバックアップが膨らむのを防ぐ)。件数判定とINSERTは `BEGIN IMMEDIATE` の同一トランザクションで行い、連打で上限を越えられないようにする |
| ユーザー選択 | 指定プレイヤーを選び、続けて変種別の統計を表示 |

常設UIは3段以内。送信者本人には報告IDだけを返し、本文は一般には出さない。**受信と同時に `#運営記録`（用意できない場合は `#運営`）へ本文つきで通知する** (Discordで手動許可した閲覧者を含む)。「報告の一覧」ボタンでも直近10件を読める。本文はコードブロックで囲む。``` をそのまま通すとブロックを抜けられ、「報告者: 〇〇」のような**通知本体に見える文面を報告者が自作できる**ため。**メンションは抑制しない** — 到達範囲はチャンネルの可視性で閉じるので、非公開の記録先に `@everyone` を書かれても閲覧者以外へは届かない。運営通知の失敗は報告の受理を止めない (DBには残っており一覧から読める)。

- **変種・卓別の試合指標**: 村/狼勝率、平均日数、平和発生確率、初日処刑、処刑成功率、初夜噛み、朝時点の平均生存狼数、JST時間帯別試合数、GM別試合数
- **試合時ランク別のプレイヤー指標**: 占い的中率、狩人が生存していた夜の護衛成功率 (`護衛成功夜 / 狩人が生存して迎えた夜`。未行動の夜も分母)、占い師/人狼の平均生存日数、役職別勝率。`rank_at_game` を使い、試合代表値 `rank_bucket` は使わない
- 表示ランクはシーズン内の相対評価。暫定ランクはランク別表示から除外し、率・平均は `STATS_MIN_SAMPLES` (既定20) 未満なら数値を出さず「試合数不足」とする
- 統計・勝率・履歴は `variant_id` で4分割し、レート・ランク・前シーズン順位は対応する `ladder_id` で3分割する。**レート対象外の卓 (`rated=false`) の試合は、全体データ・項目別ランキング・自分の統計・最近の試合のすべてから除外する** (集計SQLへ `room_id NOT IN (…)` を足す。試合番号を数えるCTEは除外しない——ログチャンネル名と番号を揃える必要があるため)。利用者向けの統計セレクトは稼働中の3変種だけを表示し、無効な13人ターン制の履歴はDBに保持して再有効化時に再表示する。途中離脱回数だけは運営用途のため全変種合算を維持する
- **項目別ランキング** (`database.LEADERBOARD_METRICS` / `get_metric_leaderboard`):
  - 各指標のSQLは `(player_id, 分子, 分母, サンプル数)` を返す。**サンプル数は指標ごとに母数が違う** (村での試合数 / 人狼で勝った試合数 / 狼予想提出の回数) ため、全体の試合数ではなくこの値へ `LEADERBOARD_MIN_SAMPLES` (既定5) を掛ける。`STATS_MIN_SAMPLES` より緩いのはこのため
  - 並びは値の降順 → **同率は母数の多い順** (試行回数が多いほうが確からしい) → `player_id` で安定させる
  - **掲載外でも閲覧者本人の生データは返す** (`viewer`)。「ランキング掲載まであと何回」を出すため。掲載されている場合だけ `viewer_position` が入る
  - 「人狼で勝った試合の生存率」は `role = '人狼'` で判定する。**狂人は含まない** (襲撃されない以上、生存率の意味が変わるため)
  - 「終了後投票の獲得票」は `rating_history.recommendation_bonus` の合計 ÷ 参加試合数。推薦票と勝利陣営票の両方を含む
- 詳細統計は導入後の試合だけが対象。既存試合を推測で補完せず、詳細値がない過去試合と明確に分ける
- **投票・CO統計** (`get_player_vote_stats` / `get_player_co_stats` / `get_co_distribution_stats` / `get_game_duration_stats`。§11のイベントテーブルを集計元とする): 投票参加率・処刑投票率 (自票が処刑先と一致した率)・狼へ投票できた率・決選参加数、CO率・初日CO率・CO役職分布・真占いとしてのCO回数・偽CO (CO役職 ≠ 実役職) の回数と勝率、占いCO人数別 (1/2/3/4+) の陣営勝率、平均・中央値試合時間 (`started_at` がNULLの試合は除外)。既存の `_stats_room_filter()` を通し、シーズン判定は `played_at` 文字列比較と揃える
- **プレシーズンの一括削除**: シーズン1を1試合目から始めるための一回きりの運用スクリプトとして `scripts/purge_preseason_stats.py` を置く。`/season_reset` はレートを圧縮するだけで試合記録を残すため、記録ごと消すにはこちらを使う。既定はdry-runで、`--execute --confirm-season1 ERASE-PRESEASON` がそろった場合だけBot本体と同じロックを取得する。自動バックアップを作成し、`integrity_check=ok`、対象テーブルの`foreign_key_check`違反なし、対象テーブルの行数が元DBと一致したことを確認してから削除する。Botが稼働中、停止状態を安全に確認できない、バックアップを検証できない、のいずれかなら削除を開始しない。`--reset-ratings` でレート・シーズン履歴も白紙にできる (0戦なのにレートだけ残る状態を避ける)。同村拒否・募集・GM村・不具合報告には触れない。**試合番号は1から振り直しになる**ので、Discordのログカテゴリも運営が手で消す前提 (Botは触らない)

### 9-1. 戦績カード画像

「自分の統計」の**「画像で見る」**ボタンから、アバター・表示名・レート/ランク・シーズン成績・得意/苦手役職TOP3 (5戦以上の役職だけを対象。得意側を先に確定し、苦手側は得意側で使った役職を除外してから選ぶため重複しない)・投票/CO統計の一部・直近試合を1枚のPNG (1500×1000前後) にまとめて描画する (`stats_image.py` / `views._build_stats_card_png`)。

- 描画はDiscordに依存しない純関数 `render_player_card(data)` が本体。`format_card_data()` がDB集計結果とDiscord固有値 (アバターのバイト列・表示名) を描画用辞書へ整形してから渡す
- **プレイヤーから見える「画像で見る」ボタン自体は既定で非表示。** データ項目とレイアウトをシーズン1開始前に確定させるための一時措置で、`config.STATS_CARD_BUTTON_ENABLED` (環境変数 `WEREWOLF_STATS_CARD_BUTTON_ENABLED`、既定 `False`) が `True` のときだけ `StatsView` にボタンを出す。描画エンジン (`stats_image.py`)・シーズン末の一括生成スクリプト (`scripts/generate_season_cards.py`)・テストはこの設定に関係なく常に動く
- フォントはリポジトリ同梱 (`assets/fonts/`) → 環境変数 `WEREWOLF_STATS_CARD_FONT_PATH` 指定ファイル → macOS標準フォント (ヒラギノ等) の既知パスの順に探す。**フォントが1つも見つからない環境ではボタン自体を出さない** (`font_available()` がFalseの間はUIから隠す。Pillow未導入でもBot全体は落とさない)
- フォントが実際に持たないグリフ (絵文字など) は描画直前に `.notdef` (通称「豆腐」) 判定で取り除き、代わりに空白の四角が出ないようにする
- 画像生成はCPU拘束処理のため `asyncio.to_thread` で実行し、同時実行数を全ギルド共有の `Semaphore(2)` で絞る。ユーザー単位で30秒のクールダウンを設け、連打によるスパム生成を防ぐ
- `scripts/generate_season_cards.py` は同じ組み立てロジックを使い、シーズン締めなどでサーバー全プレイヤー分を一括生成する運用スクリプト

### 9-2. レート推移グラフ (v0.51)

「自分の統計」の **[レート推移]** から、選択中の変種に対応するラダーのレート推移を
折れ線PNG (1400×760) で返す (`stats_image.render_rating_chart` /
`views._build_rating_chart_png`)。**[相性] と同じく、本人が自分の統計を見ている
ときだけボタンが生える** (`viewer_id == user.id`)。負けた試合の分布は既存の統計
画面より踏み込んだ情報のため、他人ぶんを引ける入口は作らない。**新しい記録は
増やさない** — `rating_history` は
シーズンリセットでも消えない (`season_half_reset` は `player_ratings` を書き換える
だけ) ため、全期間の系列がそのまま引ける。

- `database.get_rating_series()` が `rating_history` × `games` × `game_players` を
  結合し、`played_at` 昇順の `rating_before/after`・増減内訳・勝敗フラグを返す。
  既存の統計と同じく **レート対象外の卓は除外** (`_stats_room_filter()`)
- 点の色は勝敗 (勝=緑 / 負=赤)。試合数が多い場合は点だけ間引き、線は間引かない
- `season_resets` の時刻以降で最初の試合の手前へ縦の破線を引く (どこで圧縮された
  かが分かるように)。既定は直近300戦までで、超えた場合はフッタに明示する
- 描画はDiscordにもDBにも触れない純関数。戦績カードと同じ `Semaphore(2)` +
  `asyncio.to_thread` で回し、ユーザー単位30秒のクールダウンを掛ける。フォントが
  無い環境ではボタン自体を出さない (`font_available()`)

### 9-3. 相性 (v0.51)

**[相性] は本人が自分の統計を見ているときだけ生える** (`PlayerStatsVariantView` に
`viewer_id` を持たせ、`viewer_id == user.id` のときだけ追加する)。誰と組むと勝てるか
は人間関係に直結するため、他人ぶんを引ける入口は作らない (運営用の全ペア表も作らない)。

- `database.get_player_compatibility()` が `game_players` の自己結合で、相手ごとに
  **同陣営 / 敵対**の共戦数・勝数を数える
- 表示の主役は素の勝率ではなく **「その相手がいる試合の勝率 − いない試合の勝率」
  (`diff`)**。基準となる「いない試合の勝率」は、自分の陣営別勝率から
  **その相手との共戦を必ず除いて (leave-one-out)** 求め、その相手との共戦の
  陣営構成で加重する。13人卓では引いた陣営で勝率が大きく動くため、素の勝率を
  並べると「村を多く引いた相手」が全員相性◎に見えてしまう
- 基準に相手との共戦を混ぜると、相手が自分の試合の大半を占めるほど基準が実測へ
  漸近し、差がゼロへ潰れる (10戦9勝でも `diff=0.0`)。除外後に同じ陣営の試合が
  残らない相手は、陣営を跨いだ基準で誤った断定をせず**その行を表示しない**
- **共戦10戦未満 (`COMPATIBILITY_MIN_GAMES`) は表示しない。** 併せて95% Wilson信頼
  区間を添え、少数試合の偏りを相性として断定させない

### 9-4. 運営ダッシュボード (v0.51)

`#運営` の **[運営データ]** から、運営専用の指標を1枚のephemeralで返す
(`recruitment.OperationsDashboardView`)。`#運営` はボタンだけの操作盤に保つ方針
なので、面の切り替えはセレクトで行い、**押した面のSQLだけ**を都度投げる。

| 面 | 内容 | 集計API |
|---|---|---|
| 稼働 | DAU/WAU/MAU・DAU÷MAU・試合数・直近14日の日別 (試合/参加者/新規) | `get_ops_activity_stats` |
| 定着 | 新規流入 (本日/7日/30日)・2戦目到達率・週次コホートのW1/W4継続率 | 同上 |
| 離脱 | 休眠人数 (14/30/60日)・復帰人数・最終プレイからの経過分布・プレイ間隔の中央値/90%点・最長連続日数 | 同上 |
| 回転 | 募集の成立率・定員充足・満席までの時間・試合数/所要時間/終了日数・時間帯・GM別・途中離脱率 | `get_ops_throughput_stats` |
| 通知 | 「募集」DMと開始前DMの**送達失敗率**・通知オプトアウト率 | `get_ops_delivery_stats` |
| レート | レート分布 (100刻みヒストグラム)・平均/中央値・仮ランク人数・月別の平均レートと平均増減 | `get_ops_rating_health` |

- **運営面は練習卓 (`rated=false`) を除外しない。** 「人が遊んでいるか」を見る指標で
  除外すると実態からずれるため。競技面 (レート推移・相性・レート健全性の月別) は
  従来どおり `_stats_room_filter()` を通す
- 日付はすべてJST基準。`get_ops_activity_stats` は**SQLを4本** (プレイヤー要約 /
  期間内の参加行 / 期間内の試合 / 期間前の最終プレイ) だけ投げ、日別・コホート・
  連続日数・間隔はPythonで組み立てる (形の違う指標ごとにSQLを書くより本数も総処理
  時間も小さい)
- 「復帰」は直近7日にプレイし、かつその手前の最終プレイから**14日以上空いていた**人。
  直近7日が初参加の人は新規として数え、復帰には含めない
- 2戦目到達率の母数は**初参加から30日以上経った人だけ**。直近に入ったばかりの人を
  「定着しなかった人」として数えないため
- 月別平均レートは「その月に精算された `rating_after` の平均」で、在籍者全員の平均
  ではない。よく遊んだ人ほど重く出る近似だが、インフレ/デフレの傾向を見るには十分で、
  全員ぶんを月ごとに再構成するより安い

## 10. コマンド・UI一覧

**ボタンUI** (旧スラッシュコマンドを置換):
- ロビー (ローカル固定卓): 参加 / 参加取消 / GM取得 / GM放棄 / 通知 / ゲーム開始 / 次村 / GM管理 / ルール / ヘルプ。GM管理内に参加者除外と受付リセットを置く
- ロビー (GM名前村): 参加・GM登録は募集カードで受けるため、通知 / ゲーム開始 / **次村** / GM管理 / ルール / ヘルプだけを出す
- GMコントロールパネル (**ゲーム中の `#昼` の末尾に掲示**): 公開面は「GMメニュー・状況」1ボタンだけ。GMが押すと、フェーズ・日数・生存人数・投票/朝宣言の進捗・復帰待ち・API処理待ちを本人専用表示し、一時停止 / 再開 / 朝 (強制で夜を明ける) / 役職確認を締切 / スキップ (初日内通・投票発言・決戦弁明・遺言の現在枠。クロストーク通常投票の候補選択待ちでは、明示的に未投票を締切) / 次の発言へ (ターン制) / 強制終了 / リセット / プレイヤー除外を操作できる。役職と夜行動の内容は表示しない (役職確認タイム中は「役職確認の宣言」の進捗も出す)
  - 操作画面 (`GMControlView`) の `timeout` は **870秒**。GMがパネルを開いたまま待つのは8分の議論の途中で、既定の180秒だと事故が起きた頃には失効しており、一番押したい「一時停止」がDiscordの「インタラクションに失敗しました」になっていた。ephemeralの応答を編集できるのは15分までなので、その手前まで延ばして実用上失効させない
- 投票パネル (`#昼`): クロストーク通常投票では「投票参加」→発言終了→候補ボタン (番号順) →本人専用確認の順。確定すると確認メッセージが「投票しました」へ書き換わる
- 昼常設パネル (`#昼`。`VillagePanelView`、§5.3.0): CO / CO撤回 / 結果を公開 / 占い / 霊媒 / 狩人 / 人狼予想。昼夜をまたいで1枚だけ生き続け、フェーズ遷移のたびに末尾へ移す。占い・霊媒・狩人は本人ならいつでも押せて初日からの自分の記録を返し、COと公開済み結果の一覧は夜時間からも見られる (§5.3.0-1)
- 強制終了 / リセット / プレイヤー除外 / 名前村削除は確認ボタンを挟み、確認時にもGM・対象・ゲーム状態を再検証する
- **参加者を選ぶUIと参加者を並べる表示は、すべて番号順** (`models.by_number`)。投票・決戦投票・襲撃・占い・護衛・GMの除外・人狼DMの相方一覧・復帰待ち一覧などが対象。番号はランダム割り当てなので、参加順のまま出すと読めない並びになる
  - **`state.players` の dict 自体は並べ替えない。** 番号割り当て (`_start_game`) と役職配布 (`_assign_roles`) がその反復順に依存し、スナップショットも同じ順で保存・復元される。必ずソート済みのコピーを渡す
  - 例外: 進行ログは発生順 (時系列であることが目的)、決戦の弁明順はランダム (仕様)、ロビー中は番号未割り当てなので参加順
- 役職確認タイムの `#昼`: 「📩 役職を確認した」(参加者全員)。GMの確認付き強制締切はGMコントロールパネルに置く
- 夜時間終了後の `#昼`: 「🌅 朝を迎える」(0/生存人数から1枚だけ掲示。押下ごとに更新し、取消不可。生存者全員が押すと朝)。GMの強制夜明けはGMコントロールパネルの「朝」に集約
- 実人狼の役職DM: 試合中いつでも使える「🏳️ サレンダー」(生存実人狼全員の同意で村勝利・通常レート精算。途中人数は公開しない)
- **参加者が使える中断・再開申請ボタンは無い**。役職確認タイムと夜はどちらも `#昼` のパネルで、宣言ボタンを押さないことで待たせる。昼は口頭でGMに伝えて一時停止してもらう
- GMパネルはゲーム開始時に掲示し、昼・投票・弁明・遺言・夜の主要メッセージを出した後に末尾へ再配置する。ゲーム終了後は `#昼` とともにログカテゴリへ移り、ロビーパネルは `#参加受付` 再投稿時に末尾へ戻る
- **常設パネルは再起動しても新着を出さない**。`#統計`・`GM/#村作成`・`#参加受付` の常設パネルはメッセージIDを `meta` へ保存し、起動時は `get_partial_message().edit()` で同じメッセージへ上書きする (編集はDiscordの通知・未読を発生させない。`#運営` の運営パネルが以前から静かなのはこの方式のため)。保存IDが無い・そのメッセージが消えている場合だけ、従来どおりBot投稿を掃除して1枚投稿し直し、新しいIDを保存する。ロビーの掲示は `setup_channels` だけでなく **snapshotからのロビー復帰・進行中ゲームの復元・募集カードの復旧**からも走るため、起動中は `GameCog._startup_in_progress` で一括して再利用側へ倒す (呼び出し口ごとの指定では漏れる)。再利用できた起動はログに出さず、投稿し直したときだけINFOで残す (静かに戻れた起動は無言、貼り直した起動だけがログに出る)。**運用中の再掲示 (ゲーム終了後のロビーなど) は末尾に出す従来動作のまま**で、起動時だけ再利用する。ゲーム中の再起動で `#昼` のGMパネルを出し直すこと、未精算ゲームを自動復旧した際に `#統計` へ1件通知することは従来どおり (どちらも起きたことを知らせる必要がある)
- 統計: §9のボタン群 (/stats 系)
- 村・募集: `GM/#村作成` で一体作成 (村名で新規作成/既存村の使い分け) / 村名変更 / 村を削除 (複数あるときはセレクトで対象を選ぶ) / 各名前村の `#参加受付` で参加 / 参加取消 / ゲーム開始 / 🔔通知 / 主催者メニュー（備考・ゲーム形式・一括連絡・**参加者を除外**・募集廃止・**前回設定で参加受付を再開**）/ ルール / ヘルプ / **募集** (DM一斉通知)
- **通知設定**: `🔔通知` から開くephemeralパネルで3項目を個別にON/OFF切替 (「通知の許可」「村ができた時に通知」「この村を通知を受け取る」)。「通知の許可」をOFFにすると他の2項目は実質無効になる (`allow_notifications=1` が条件のため)。「村ができた時に通知」は募集カード公開時の `@通知` ロールメンションの購読、「この村を通知を受け取る」は主催者が押す**「募集」**ボタンのDM購読
- **「募集」ボタン**: 主催者メニュー内ではなく募集カード2段目に置く常設ボタン。押すと「この村を通知を受け取る」を有効にしている購読者へDMで村を知らせる。**1つの募集につき1日1回**まで (`recruitment_calls` の `UNIQUE (recruitment_id, called_on)` で担保)。送信は `WEREWOLF_RECRUITMENT_CALL_DM_INTERVAL_SECONDS` (既定0.7秒) 間隔でペースを絞り、1人が1日に受け取れる件数は `WEREWOLF_RECRUITMENT_CALL_DM_DAILY_LIMIT` (既定5件) を超えない (超過分は送達台帳へ `skipped_cap` として記録し送信しない)
- 運営: 報告の一覧 / 被拒否数 / 途中離脱の一覧 / 9人均衡監視 / **運営データ** (§9-4) / GM解除 / 募集の強制削除 / **村の強制削除** (村主を問わず選択削除。受付中の募集も一緒に締め切る) / **シーズンリセット** (`/season_reset` と同じ処理。どちらの入口でも本人だけの最終確認を挟む)

**スラッシュコマンド** (3つのみ):
| コマンド | 権限 | 内容 |
|---|---|---|
| `/season_reset [note]` | サーバー管理 | シーズンリセット=ハーフリセット (最終確認あり。`#運営`のボタンからも同じ入口) |
| `/private_room_create` | GMまたは仮GMロール | 名前村と募集の作成UIを開始 |
| `/private_room_delete` | 村主本人のGMまたは仮GMロール。ロール喪失時は村主本人かつ「サーバーを管理」権限で救済可能 | 名前村と受付中募集を削除 (確認後に実行) |

## 11. 技術構成

### ファイル構成
```
bot/
├── bot.py              エントリーポイント (ログ/SSL/Cogロード/コマンド同期)
├── command_sync.py     スラッシュコマンドの差分同期・global監査
├── config.py           定数・卓定義・レート/ランク設定
├── models.py           Player / GameState
├── game.py             GameCog (全卓管理・イベントdispatch・スラッシュコマンド)
├── room_runner.py      RoomRunner (1卓のゲーム進行ロジック)
├── permissions.py      RoomPermissionMixin (チャンネル権限管理)
├── views.py            全ボタン/セレクトUI・ヘルプ・embed生成・#昼常設パネル (VillagePanelView)
├── recruitment.py      名前村一体型の募集UI・通知・同村拒否・運営UI・通知設定・「募集」DM配信
├── database.py         SQLite (aiosqlite, WAL)。data/werewolf_stats.db
├── stats_image.py      戦績カード画像 (PNG) の描画。Discord非依存の純関数
├── rating.py           レート計算・相対ランク
├── room_config.py      `.env` のローカル固定卓を厳格に検証して読み込む
├── sounds.py           シーン切替SE (サイン波をメモリ合成してVCで再生)
├── tests/              ユニットテスト
├── SPEC.md             本書
├── README.md           クイックガイド (セットアップ/起動/テスト)
├── requirements.txt    依存パッケージ (discord.py==2.7.1 ほか)
├── CHANGELOG.md        プレイヤー向けの変更履歴 (そのままDiscordへ貼れる粒度)
├── scripts/            run_bot.sh / stop_bot.sh / setup_venv.sh /
│                       start_bot_detached.py / verify_runtime.py /
│                       check_se_playback.py / purge_preseason_stats.py (一度限り) /
│                       generate_season_cards.py (シーズン締め用の戦績カード一括生成) /
│                       *.applescript / run_checks.sh
├── data/               werewolf_stats.db + backups/ + lib/ (同梱libopus。
│                       いずれもgitignore対象。
│                       24時間ごと・/season_reset 前に自動バックアップ、
│                       ラベル別に10世代保持。作成時に wal_checkpoint して
│                       -wal / -shm を残さず、世代を捨てるときは本体と
│                       一緒にサイドカーも消す)
├── logs/               bot.log (5MB×3世代ローテーション) / launcher.log
└── .github/workflows/  ci.yml (GitHub push時の自動チェック)
```

### 実装上の要点
- **実行環境**: Python 3.14（3.14.6で検証）。Apple Siliconでは `/opt/homebrew` のarm64 PythonをPATH探索より優先し、Rosetta上のx86_64 Pythonを拒否する。直接依存は `requirements.txt`、推移依存を含む検証済み完全固定版は `requirements-lock.txt` で管理する。ただし `discord.py[voice]` 2.7.1 が `PyNaCl<1.6` を要求するため、PyNaClだけは互換範囲内の最新1.5.0を使う
- **API負荷対策**: ラッパー経由のメンバー編集・権限変更と一部のメッセージ・DMは `Semaphore(5)` で全卓共有のスロットリングを行う。募集通知・一括連絡などの直接送信は対象外。権限上書きは差分がある時だけ送信
- **コマンド同期**: 管理用スラッシュコマンドは実定義からスキーマhashを作り、初回・定義変更時だけguildへ同期する。通常の再起動はguildの現状をGETで照合し、外部削除・変更があればその起動時に同期して自己修復する。globalコマンドはローカル定義を毎回空にし、前回監査から7日以上経過した次回起動時に確認して、存在するときだけ空同期する。状態はApplication IDとともに `bot_meta` に保存し、キャッシュ読込失敗時は従来どおり同期する
- **押下の計測**: 全員が同時に押すボタン (朝を迎える / 役職を確認した / 投票の確定) は `views.InteractionTimer` で段階ごとの所要時間を測る。`ack` (Discordへの応答) → `lock` (卓の `action_lock` 取得) → `state` (状態更新とDB保存) → `reply` (本人への返信) の4区間を記録し、合計が `SLOW_INTERACTION_SECONDS` を超えたときだけWARNINGで出す。「押したのに反応しない」の原因が、Discordの応答なのか、他の処理が握っている卓ロック待ちなのか、DB保存なのかをログだけで切り分けるため。平常時は人数分×毎晩のログを出さない
- **状態永続化**: フェーズ遷移に加え、役職確認と同時開始する初日内通の完了、クロストーク通常投票の待機列・発言終了・候補選択・確定、CO結果公開、夜の行動、朝待機の受付開始・宣言、サレンダー同意・成立・村勝利意図、死亡・ミュート所有・開始処理の進捗を卓スナップショットへ保存する。勝敗境界に使うboolやID配列は型を強制変換せず検証し、不整合は安全側へ停止する。payloadは現行schema version 1を必須とし、欠損・旧version・破損した卓だけを自動変換せず隔離して、他卓の復元を継続する
- **外部副作用の再実行**: 死亡通知・権限変更はoutbox、名前村作成/改名/削除は進捗journalとして保存し、Discord APIとDBの間で停止しても再調停する。通知は欠落防止を優先するため障害窓では重複する場合がある
- **レート整合性**: 勝敗結果と活躍ボーナスの材料 (`bonus_payload`) を先に未精算キューへ保存し、ゲーム履歴とレート更新を同一トランザクションで冪等精算する。終了後投票も1試合・1投票者・1種別 (`kind`) の一意制約と集計済み状態を同一トランザクションで更新し、二重加算を防ぐ。レート計算・投票集計・シーズンリセットは `rating_lock` で直列化する
- **移行**: 旧版の `private_rooms` にある `UNIQUE (guild_id, owner_id)`（1人1村）は、**現行必須スキーマの検証を通した直後**にテーブル再構築で外す。検証は必要な制約の不足だけを拒否し、この旧UNIQUEは追加制約として許容するため、旧DBを壊さず移行対象として判定できる。外さないままだと2村目のINSERTで落ちる。村名の一意制約は維持する。移行が対象にするのは旧版が作った**通常のUNIQUEだけ**で、条件付きUNIQUEは検証側と同じく制約の代用と認めず、再構築の根拠にもしない (手で足された索引を移行のついでに落とさないため)。再構築は現行定義でテーブルを作り直すため、旧版が残した**nullableな余分列は新テーブルへ引き継ぐ**。DEFAULTなしNOT NULLの余分列があるときはDDLへ触れずに戻り、再検証に起動停止を任せる (黙って列を捨てない)。この処理を削除できるのは、本番DBだけでなく今後復旧対象にする全バックアップでも旧UNIQUEがないことを確認し、v0.46以前のDBを直接復元しない運用へ移行した後に限る。
- **DBスキーマ**: games / game_players / **game_stats** / player_ratings / rating_history / game_recommendations / season_resets / rating_snapshots / room_states / room_state_quarantine / game_settlements / pending_unmutes / private_rooms / **recruitments** / **recruitment_entries** / **recruitment_notification_deliveries** / **player_blocks** / **feedback_reports** / **bot_meta** / **game_co_events** / **game_co_results** / **game_vote_events** / **game_night_actions** / **user_notification_prefs** / **recruitment_calls** / **recruitment_call_deliveries** / **game_turn_events**
  - v0.51以降で追加した8テーブルは**発生した瞬間に追記する生ログ**。`game_id` は精算時に後埋めするため試合開始時点ではNULL、進行中は `(guild_id, room_id, game_run_id)` がキー。UNIQUE制約は張らず、撤回・やり直しは行を消さず `event_type` を追記して残す
    - `game_co_events`: CO宣言・撤回 (`event_type` = 'CO' / '撤回')
    - `game_co_results`: 占い師・霊媒師・狩人の結果自己申告 (`event_type` = '公開' / '取消')。村パネルの「結果を公開」で追加し、訂正時も履歴を残す
    - `game_vote_events`: 本投票・決選投票の生ログ (変更前の票も残し、最新は `event_seq` の最大値)
    - `game_night_actions`: 占い・護衛・襲撃投票/確定・霊能の生ログ。v0.51で **`初日白`** (占い師の初日ランダム白) と **`人狼予想`** (死亡時に挙げた相手を1人1行。`result` に相手の真の陣営) を追加した。`game_players.wolf_guess_hits` は正解数しか残らず、誰を挙げたかを後から追えないため
    - `user_notification_prefs`: ユーザー単位の通知設定3項目 (`allow_notifications` / `notify_on_create` / `notify_on_call`)
    - `recruitment_calls`: 募集カードの「募集」ボタン発火記録。`UNIQUE (recruitment_id, called_on)` で1村1日1回を担保
    - `recruitment_call_deliveries`: 「募集」DMの送達台帳 (開催前DMの `recruitment_notification_deliveries` とは別台帳)
    - `game_turn_events`: ターン制の割り込み受理ログ (現状 `event_type` = '割り込み' のみ。将来パス等を足せるよう汎用名にしてある)。誰の発言枠に (`speaker_id` / `speaker_number`) 誰が割り込んだか (`actor_id` / `actor_number`) を記録する
  - `games.started_at` / `game_settlements.started_at`: 試合開始時刻 (NULL許容)。平均試合時間の集計に使う
  - **試合番号の表示**: `games.game_id` はAUTOINCREMENTで欠番が出るため、UIには出さず**サーバー内の通し番号 (古い順に1から)** を `ROW_NUMBER()` で振って表示する (`database._GAME_SEQ_CTE`)。欠番の原因は開発中の検証で消費したぶん (本番DBへの書き込みガードは `34c77e5` で後から導入)。**廃村 (`force_end`) は精算しないので `games` に入らず、番号も消費しない**。精算がロールバックされた場合も、SQLiteは採番カウンタを巻き戻すので消費しない
    - 復元は**`-wal` / `-shm` を先に削除してから**本体を差し替える。残したままだとSQLiteが古いWALを再生し、エラーを出さずに移行後のデータが復活する
  - `games.gm_id`: 進行GM
  - `games.variant_id` / `games.ladder_id`: 試合時の変種と対応ラダー
  - `games.base_room_id`: 将来の増設卓を固定卓へ正規化するキー。現在は `room_id` と同値
  - `games.recruitment_id`: 募集経由の試合だけ募集ID、直接開始はNULL
  - `player_ratings`: 主キーを `(player_id, guild_id, ladder_id)` にし、13人・9人クロストーク・9人ターン制の現在値を混ぜない。`rating_snapshots` も `ladder_id` を持ち、共通のシーズンリセットIDの下へラダー別スナップショットを残す
  - `rating_history`: `ladder_id` と `variant_id` を両方持つ。現在値の更新先はラダー、履歴の絞り込みは変種を使う
  - `game_settlements`: `variant_id` / `ladder_id` に加え、村・狼プール、狼予想枠、終盤判定を試合終了時にスナップショットする。精算再試行時も最新設定ではなく、その試合で確定した値だけを使う
  - `game_players`: 死亡日・死因・試合時表示ランク・暫定フラグ・**狼予想提出の的中数** (`wolf_guess_hits`。NULLは未提出/対象外で、的中率の分母にも入らない)
  - `game_stats`: 日数、平和、護衛確定回数/成功、占い結果、処刑、初夜噛み、日別生存狼数、将来用ランク中央値
  - `recruitments` / `recruitment_entries`: 卓の時間予約、募集条件、参加・補欠、GM、募集単位の通知・メッセージ状態
  - `recruitment_notification_deliveries`: 募集×参加者ごとの開催前DM送信済み・恒久拒否状態
  - `player_blocks`: ギルド単位の片方向同村拒否
  - `init_db()` は新規DBへ現行テーブルを作成する。既存DBは書き換える前にv0.40の必須テーブル・列型・NULL可否・DEFAULT・主キー・GM名前村の一意制約・必須外部キー・CHECK制約と孤児行/制約違反行の有無を検証し、未移行または破損したスキーマなら明示的に起動を中止する。v0.39以前からの直接アップグレードはサポートせず、v0.40へ移行済みのバックアップまたは新規DBを使う
  - v0.40移行で残っていた旧列 (`private_rooms.role_id` / `role_name`、`recruitments.allow_beginner` / `rating_limit`)・空の旧テーブル (`private_room_members`)・完了marker3件は、Bot停止・別名バックアップ・コピー上での検証を経て**物理削除済み**。現行DBのスキーマは `init_db()` のDDLと一致する。ただし削除前に取得した旧バックアップを戻す可能性があるため、余分なnullable列と未使用の旧テーブルに残る孤児行は引き続き起動条件にせず、起動時にも自動DROPしない。今後同種の残骸が生じた場合も、物理削除は同じ手順の個別メンテナンスとして行う
  - 現行DBでも、廃止した共有ラダー`l9`の再混入、9人変種と`l9_cross` / `l9_turn`の不一致、試合とレート履歴の変種・guild不一致、未終了募集と村主のGM名前村の不一致、募集参加区分と自己ブロックの制約違反を起動時に検査する。不一致を自動アーカイブして隠さず、DBを変更する前に起動を止める。すべての検索索引は定義が現在と異なる場合だけ同一savepoint内で再作成し、1件でも失敗したら索引変更をすべて戻す。通常起動では毎回DROP/CREATEしない
  - 復元候補は停止中に取得したコピーへ `PRAGMA integrity_check` / `foreign_key_check` と現行スキーマ検証を行い、旧`l9`が対象テーブルにないこと、9人2変種の履歴・試合・未精算結果が対応ラダーへ一致することを確認する。本番DBそのものをシミュレータへ渡さない
- **必要なBot権限**: チャンネル管理 / ロール管理 / ニックネーム管理 / メッセージ管理 / DM送信 / **メンバーをミュート** (サーバーミュートによる発言制御に必須)。ランクロールよりBotロールを上位に置くこと

### 起動・停止 (デスクトップの .app)
- **人狼Bot起動.app**: 所有者限定のruntimeディレクトリとファイルロックで多重起動を防止し、`scripts/start_bot_detached.py` 経由で完全デタッチ起動。Discord初期化とチャンネル復元が終わりready markerが出るまで成功表示しない
- **人狼Bot停止.app**: markerのPIDだけで信用せず、Python実行ファイル・`bot.py`の絶対パス・作業ディレクトリを照合したプロセスだけへ TERM → 5秒待ち → KILL。無関係なプロセスは停止しない
- `DISCORD_GUILD_ID` で管理対象を1サーバーに固定する。ランチャー起動と直接起動は同じruntime lockを共有する
- `setup_venv.sh` は同じ厳密なプロセス照合でBot稼働中か確認し、稼働中または確認不能ならvenvを変更しない。venv更新は停止 → セットアップ → 起動の順で行う

### テスト
- 変更中の高速確認は関係するユニットテストを使う
- `scripts/run_checks.sh`: コミット前の完全確認。Apple Siliconネイティブ実行検査 + 作業ツリー内の全Pythonファイルのコンパイル + 依存整合 + 全ユニットテスト
- ゲームシミュレーションは通常チェック・CI・リリース作業では実行しない。ユーザーから明示的に依頼された場合だけ実行する
- `.github/workflows/ci.yml`: GitHubへのpush/PRで同種のチェックがCIで走る。稼働Macと同系統の `macos-26` / Apple Silicon (arm64) を固定し、Python 3.14、Homebrewのarm64版libopus・DAVE、追跡済みPythonのコンパイル、依存整合、全ユニットテストを検証する

### 段階公開のリリースゲート

1. 自動テストで変種定義、13/9人の配役・定員、3ラダーの分離、`elo_delta`ゼロサム、現行DBスキーマの検証と再起動時の不変性を確認する。既存13人クロストークは `DEFAULT_VARIANT_ID` の互換エイリアスと従来テスト群を残し、役職数・昼夜・投票・精算の回帰を検出する
2. 停止中に取得した現行DBコピーで整合性検証を行い、復元可能なバックアップを別名で保持する。未移行の旧DBは現行版で直接更新せず、v0.40移行済みDBまたは新規DBを使う。ロールバックする場合はBotを停止したまま、互換性が確認できたコードとDBをセットで戻す
3. 名前村で公開3形式を選び、作成→参加→受付中の形式変更→開始→昼→投票→夜→終了を少数の実地試験で確認する。募集カード、同村拒否、補欠、DMに加え、未参加者が公開VCへ接続してもゲーム中は発言・VC内チャット不可であること、参加取消後も観戦を続けられることを同じ `#参加受付` で確認する
4. 13人ターン制は無効のまま保ち、将来有効化する前に設定変更時点で未解決snapshot・募集がないことを確認する
5. ログに永続化停止、Discord 403/429、二重精算、ラダー混在がなく、実試合の統計・レートを運営者が照合する

## 12. v2からの主な変更点

| 項目 | 旧仕様 (v2) | 現行仕様 |
|---|---|---|
| 構成 | 単一ゲーム | GMの名前村 (1人で複数保有可) +任意のローカル固定卓。名前村では公開3形式を選択でき、13人ターン制は実装済み未公開 |
| レート | なし | 変種別固定プール+13人/9人クロストーク/9人ターン制の3ラダー+相対ランク+共通シーズン境界 |
| 人狼の相談 | #狼の巣チャンネル | DM自動中継 |
| 投票 | 棄権不可・全員完了待ち | クロストークは「投票参加」を押した順に1人30秒（本人終了可）→発言終了SEと約2秒→候補選択・確定。候補選択は無制限で自動棄権なし。ターン制は従来どおり一斉投票 |
| 再同票 | 未定義 | ランダム処刑 |
| 霊媒結果 | 役職を通知 | 人狼/村人の2値 (占いと統一) |
| 夜の時間切れ | 廃村 | 狩人以外は未行動スキップで続行（狩人は護衛必須） |
| DM拒否者 | 警告のみ | 参加不可 (役職DM失敗は中断) |
| タイマー | Discordタイムスタンプ | メッセージ編集式 (一時停止対応のため) |
| コマンド | スラッシュ14個 | ボタンUI+スラッシュ3個 |
| 障害復旧 | なし | スナップショット復元+途中再開 |
| プレイヤー退出 | 未定義 | 死亡扱い+即時勝敗判定 |
| 霊界チャンネル | 設置しない | 各卓にゲーム中のみ `#霊界` を設置 (死亡者+観戦者専用、生存者には不可視) |
