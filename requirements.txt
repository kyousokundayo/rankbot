# 2.7系のVoiceClientはDAVE E2EEが必須。voice extraで
# PyNaClとdaveyを必ず解決し、下の直接pinでも欠落・意図しない更新を防ぐ。
discord.py[voice]==2.7.1
aiohttp==3.14.3
aiosqlite==0.22.1
python-dotenv==1.2.3
certifi==2026.7.22
# discord.py 2.7.1 の voice extra が PyNaCl<1.6 を要求するため、
# 1.6.2 ではなく制約内の最新 1.5.0 を使う。
PyNaCl==1.5.0
davey==0.1.6  # discord.py 2.7系のDAVE E2EE実装
# 戦績カード画像 (stats_image.py) の描画専用。Python 3.14 対応を確認済み。
Pillow==12.3.0
